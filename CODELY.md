# CODELY.md — colab_listening_b_web

## Project Overview

本地 Web 管理界面，用于管理和运行英语听力视频生成 Pipeline。通过 FastAPI Web UI 一键启动/继续/停止视频生成流程，实时查看日志和进度，可视化管理所有参数、主题和运行历史。

**核心能力：** 将英语对话脚本（LLM 生成）→ TTS 配音 → AI 图片/视频生成 → FFmpeg 合成 → 4K 超分辨率 完整流程封装为可交互的 Web 应用，支持断点续传、单步调试模式、角色复用。

**技术栈：** FastAPI + Jinja2 SSR + Tailwind CSS + HTMX + 原生 JS

## Architecture

```
colab_listening_b_web/
├── app/                          # Web 应用层
│   ├── main.py                   # FastAPI 路由 + API 端点（页面/配置/Pipeline/主题/运行历史/角色复用）
│   ├── config_manager.py         # 参数规格定义(PARAM_SPEC)、配置读写、预设管理、CLI 参数构建、MCP Token 检测
│   ├── pipeline_service.py       # 核心：直接 import pipeline 模块，后台线程执行，步骤进度追踪，单步模式
│   ├── pipeline_runner.py        # 兼容性包装（re-export get_service）
│   ├── templates/                # Jinja2 HTML 模板（base + dashboard + config + topics + runs + gallery + partials/）
│   └── static/                    # CSS (style.css) + JS (app.js)
├── pipeline/                     # 独立 Pipeline 代码（自包含，不依赖外部项目）
│   ├── pipeline.py               # 主编排器：7 步流程 + argparse CLI 入口
│   ├── llm_client.py             # LLM 客户端（SenseNova / OpenAI 兼容 API；listening 脚本生成）
│   ├── mcp_client.py             # TJGenerators MCP HTTP 客户端（多 Token 轮换，积分耗尽自动切换）
│   ├── tts_engine.py             # Kokoro TTS 引擎（英文+中文，音量归一化，语音修复）
│   ├── tts_pipeline.py           # 批量 TTS 生成（对话+旁白+词汇）
│   ├── image_gen.py              # AI 图片生成（角色/场景/对话图/姿势图，含断点续传检查）
│   ├── clip_gen.py               # Seedance2 视频片段生成（任务构建+轮询+下载+重试）
│   ├── grouping_b.py             # 对话行分组（连续行合并为一个视频片段）
│   ├── group_audio.py            # 分组音频拼接
│   ├── timeline.py               # 时间轴构建（listening 4章结构）+ SRT 生成
│   ├── timeline_enrich.py        # 时间轴补全（audio_dur/duration）
│   ├── video_compose.py          # FFmpeg + Pillow 视频合成（original/original_static 模式）
│   ├── original_cutout_compose.py # Original Cutout 合成（人物抠图停格动画）
│   ├── stop_motion.py            # 定格动画渲染（多姿势+光流插帧，quest/cutout 共用）
│   ├── thumbnail_gen.py          # YouTube 缩略图 + 元数据生成
│   ├── matting.py                # MODNet AI 抠图（本地 ONNX，matting_engine 配置，白度法保留可选）
│   ├── sr_upscale.py             # realesr-animevideov3 AI 4K 超分（torch CUDA fp16 双管道流式，upscale_engine 配置）
│   ├── media_utils.py            # FFmpeg/Pillow 共享工具（concat/字幕烧录/loudnorm/分辨率探测）
│   ├── topic_manager.py          # 主题随机选择 + 防重复（topics.json + used_topics.json）
│   ├── checkpoint.py             # 断点续传（checkpoint.json 保存/加载/步骤完成检查）
│   ├── topics.json                # 主题库（分类 JSON）
│   └── quest/                     # Quest 结构变体（任务听力模式）
│       ├── llm_client_quest.py   # Quest 脚本生成（48行，4阶段：buildup→core→reveal→review）
│       ├── timeline_quest.py     # Quest 时间轴 + SRT
│       └── video_compose_quest.py # Quest 视频合成（定格动画，角色姿势图）
├── configs/                      # 配置文件 + 预设
│   └── default.json              # 默认配置（含所有参数）
├── requirements.txt              # Python 依赖
└── run.bat                       # Windows 启动脚本
```

### 关键设计

- **直接导入而非 subprocess：** `pipeline_service.py` 直接 `import` pipeline 模块，在后台线程中调用 `_step0_script()` ~ `_step6_4k()` 函数，实现实时进度追踪和中间结果访问。stdout 被 `_LineBuffer` 逐行捕获转发到 SSE 日志流。
- **单步模式：** `_step_mode` 在每步完成后暂停，等待用户点击"继续"，便于调试和检查中间产物。
- **断点续传：** 每步完成后写 `checkpoint.json`，`--resume` 时跳过已完成步骤。完整运行后清除 checkpoint。
- **角色复用：** 支持从之前的运行中复制角色图片和描述，或用自定义描述覆盖（`character_source` + `character_reuse` + `character_fixes`）。
- **角色套装：** 整套角色配置（复用源/复用模式/素材库绑定/Qwen音色/描述覆盖）可命名保存到 `configs/character_sets.json`，控制台「复用角色」区下拉一键应用到当前模式（同名单模式覆盖更新；跨模式应用有确认警告）。
- **多 Token 轮换：** MCP 客户端支持多个 OAuth Token，积分耗尽时自动切换到下一个。

## Pipeline Steps

| 步骤 | 函数 | 说明 |
|------|------|------|
| Step 0 | `_step0_script` | LLM 脚本生成（SenseNova / OpenAI 兼容 API），含验证+重试 |
| Step 1 | `_step1_mcp` | TJGenerators MCP 初始化（多 Token 轮换） |
| Step 2 | `_step2_images_tts` | 并发：AI 图片生成 + TTS 配音（后台线程） |
| Step 3 | `_step3_clips` | Seedance2 视频片段生成（original_static/original_cutout/quest 模式跳过） |
| Step 4 | `_step4_timeline` | 时间轴 + SRT 字幕构建 |
| Step 4.5 | `_step45_thumbnail` | YouTube 缩略图 + 元数据生成 |
| Step 5 | `_step5_compose` | FFmpeg + Pillow 最终视频合成 |
| Step 6 | `_step6_4k` | 可选 4K 超分辨率（`--no-4k` 跳过） |

### 视频结构模式

- **original** — 4 章视频片段模式（对话用 Seedance2 AI 视频，含跟读练习）
- **original_static** — 静态图片模式（4 章、无视频生成、逐行对话配图，支持 none/landing/stop_motion 动画类型）
- **original_cutout** — 人物抠图模式（原版 4 章 + quest 式角色抠图停格动画：char_a/b 各 8 姿势图集）
- **original_sprite** — 固定机位模式（行为族=original_cutout，强制 sprite_sequence；对话双角色按身份定死左右位 char_a 左/char_b 右，不随说话者换位；主持人段/Ch3 静态帧与其他 cutout 族一致）
- **quest** — 任务听力模式（48 行对话，4 阶段结构，定格动画，3+1 角色）

### 标题策略（title_quote 引语钩子）

- LLM 脚本新增 `title_quote` 字段：从对话中逐字挑选最抓耳的一句（<10 词）
- `youtube_title_en` 优先用引语钩子模式：`"Pump Number 3, Please" — Paying Inside at an American Gas Station`
- `youtube_title`（繁中）可用引语的繁中翻译开头（「三號加油機，麻煩了！」）

## Building and Running

### 启动 Web 服务

```bash
# 安装依赖
pip install -r requirements.txt

# 启动（方式一：run.bat，端口 8765，仅本机访问）
run.bat

# 启动（方式二：手动 uvicorn，可自定义端口和访问范围）
python -m uvicorn app.main:app --host 0.0.0.0 --port 59510
```

浏览器访问 http://localhost:8765（run.bat）或 http://localhost:59510（手动）

### 直接运行 Pipeline（CLI）

```bash
cd pipeline
python pipeline.py --topic "At the Pharmacy" --cefr A2 --output ./output --mcp-tokens TOK1,TOK2 --structure original
python pipeline.py --resume  # 断点续传
```

### Pipeline 外部依赖

- **FFmpeg** — 必须在 PATH 中（视频合成、音频处理、4K 超分辨率）
- **TJGenerators MCP** — AI 图片/视频生成（需 OAuth Token，配置在 `mcp_tokens` 或 `~/.codely-cli/mcp-oauth-tokens.json`）
- **Kokoro TTS** — 本地 TTS 引擎（首次使用自动从 HuggingFace 下载模型，Windows 自动用 hf-mirror.com）
- **SenseNova API Key** — LLM 脚本生成（或使用 OpenAI 兼容 API）
- **Pillow (PIL)** — 静态帧渲染、字幕 PNG 叠加、缩略图生成

## Development Conventions

### 代码风格

- Python 3.12+ 类型注解（`dict[str, Any]`, `list[str]`, `str | None`）
- FastAPI 路由用 `async def`；Pipeline 执行在 `threading.Thread` 中同步运行
- 配置参数集中定义在 `PARAM_SPEC` 字典（含 default/type/group/label/help/options）
- Pipeline 模块自包含：不 import 外部项目，所有依赖在 `pipeline/` 目录内
- 中文注释和日志（面向中文用户），代码标识符用英文
- Windows 兼容：`sys.stdout.reconfigure(encoding='utf-8')` 处理控制台编码

### 前端约定

- Jinja2 模板继承 `base.html`（侧边栏 + 内容区）
- Tailwind CSS（CDN）+ HTMX 用于动态交互
- SSE（Server-Sent Events）实时推送日志和状态
- API 路径约定：`/api/{domain}/{action}`（如 `/api/run/start`, `/api/config/save`）

### 配置管理

- **按模式过滤：** 配置页只渲染当前模式实际消费的参数（`PARAM_SPEC` 条目 `modes` 标注，缺省=全模式生效；`effective_param_spec(mode)` 过滤，`param_effective_in_mode` 查询）。生效性以管线消费方为准——original: clip_duration/clip_concurrency；cutout: animation/dh_*/host_character/render_fps/workers/matting_engine；quest: quest_beat_lines/render_fps/workers/matting_engine；quest 无 Ch3/中文（practice_duration/ch3_*/character_zh_voices 仅 original*）；static 无特有项（animation 强制 none）。被过滤键保存后回落默认值（load_mode_config 合并补全），CLI 构建不受影响；新增管线参数须同步维护 modes 标注
- 默认配置：`configs/default.json`（所有参数的当前值）
- 预设文件：`configs/{name}.json`（保存的参数组合）
- 配置合并策略：`get_default_config()` → `merged.update(saved)`（新增参数自动补全默认值）
- 敏感文件：`configs/*.local.json` 被 `.gitignore` 忽略

### Git 工作流

- 每次修改完代码后，立即 commit 并 push 到 GitHub

### Pipeline 关键约束

- 时间轴 gap 必须为 0.0（FFmpeg concat 无缝，非零 gap 导致 SRT 漂移）
- TTS 音频必须 `loudnorm` 归一化（跨语音音量一致）
- `setpts` 变速公式：`target_dur/vid_dur`（>1 减速，<1 加速），需配合 `fps=25` 滤镜
- 中文文本在 Windows 上用 Pillow 渲染（FFmpeg drawtext 不支持中文）
- IPA 音标用 `C:\Windows\Fonts\cambria.ttc`（msyh.ttc 渲染为方框）
- Seedance2 视频最大并发 5 个任务，超出需分批创建
- MCP 下载文件需验证大小 > 500KB（防止静默失败的小文件）
- `probe_resolution` 解析 ffprobe csv 输出（`1080,1920` 逗号分隔，非 `x` 分隔）— 字幕叠加按实测画布渲染
- 本地 AI 引擎两件套（2026-08-30 上线；同日 digital_human 数字人引擎已应用户要求整体删除，代码/配置/UI 均无残留）：MODNet 抠图 `H:\models\modnet\`、AI 超分 `H:\models\upscaling\`（ModelScope 镜像下载，SHA256 校验过）；全部走「配置选项 + 原方法保留」：matting_engine=auto/modnet/white_threshold、upscale_engine=ffmpeg/ai（默认 ffmpeg）；无 MODNet 时整帧 cover 兜底
- 主题库矩阵策略：每个场景从多角度开采（顾客操作/员工POV/品牌具体/首次体验/小麻烦/文化问答），`pipeline/topics.json` 现 726 条 26 分类；topics_ai.py 生成 prompt 已内置矩阵思维

## Codely Structured Memories

### User

### Feedback
- [2026-08-26 20:02:43] User wants every code change committed and pushed to GitHub immediately after modification. **Why:** user explicitly asked to enforce this as a standing rule in CODELY.md. **How to apply:** after completing any code edit in this project, run git add + commit + push right away without being asked.
- [2026-08-27 01:48:36] CODELY.md 正文无法用 replace/write_file 直接编辑（工具报 "memory file" 错误），须走 Python 脚本字节级替换；本项目及多数仓库文件为 CRLF 换行，search 输出看不出 \r，模式串要先 python repr 探针确认行尾，并把 pattern 中 \n 统一转为 \r\n，且每处替换加 assert count==1 防静默错位。
- [2026-08-27 22:28:22] [2026-08-27 02:02] Windows 验证类命令的执行方式：不要用超长 `python -c "..."; ...` 单行，改用 write_file 写临时 .py（必要时配 .cmd）到系统临时目录（%TEMP%≈C:\Users\Administrator\AppData\Local\Temp）再 run_shell_command 执行，跑完立即 del 删除。**Why:** 用户明确指示（"别用powershell -c了 把刚才那条py_compile换成.cmd临时脚本或.bat来跑 写到系统临时目录，跑完删除"）；另注意 PowerShell 不允许 `&` 串联命令（ParserError），多条命令用 `;` 分隔。**How to apply:** 本项目/Windows 会话中做 py_compile 批量校验、一次性冒烟测试时套用此模式。[2026-08-27 22:28 补充] 带参调用 .cmd 用 `& "C:\...\pycheck.cmd" "arg1" "arg2"`（`cmd /c "path" "arg"` 的引号会被拼成一个路径报 CommandNotFound）；参数化 .cmd 内用 `:loop / if "%~1"=="" goto done / shift / goto loop` 一次校验多文件，避免每次重写临时脚本；curl.exe 输出不要直接管道给 python -c（编码/BOM 解析失败），先 -o 存文件再解析。

- [2026-08-27 19:59:24] [2026-08-27] 用户决策：4K 步骤必须保持在字幕烧录之后执行（scale=3840:2160 不改）——字幕随 4K 等比放大是期望行为，否则字幕在 4K 下过小。**Why:** 审查报告曾建议改为先4K后字幕+等比缩放，用户明确否决。**How to apply:** 后续审查/重构不要再把“4K 放大 Pillow 字幕”当问题报告或改动 _step6_4k 的执行顺序。
- [2026-08-27 19:59:37] [2026-08-27] replace 工具在本仓库（CRLF 文件）使用教训：old_string 以换行结尾而 new_string 没有时，会把下一行合并进上一行造成 SyntaxError（当天踩坑2次：pipeline.py 的 checkpoint print 行、qwen_tts_engine.py 的 def 行）。**Why:** 尾部换行结构不一致被静默合并。**How to apply:** 编辑时让 old_string/new_string 保持相同的首尾换行结构；删大段代码优先用临时 python 脚本 + 唯一锚点正则 + assert 唯一性断言，写盘放最后保证可安全重跑；每次编辑后立即 py_compile 验证。
- [2026-08-30 09:24:18] [2026-08-30] 验证 config.html 渲染字段时勿用 `data-field="{key}" in html` 子串断言——JS 里 querySelector('[data-field="animation"]') 字符串会造成误报。**Why:** 模式过滤测试首轮因此误判泄漏。**How to apply:** 用元素级正则提取真实表单控件：`re.findall(r'<(?:input|select|textarea)\\b[^>]*?data-field="([^"]+)"', html, re.DOTALL)` 再做集合断言（[^>]* 默认跨行，多行 select 标签也能匹配）。
- [2026-08-30 19:18:30] image_provider=sensenova 的 SENSENOVA_API_KEY 注入已与 LLM provider 解耦（commit 4d142bb）：_set_env 在 provider 分支之外统一注入 config.sensenova_api_key，CLI --api-key 同样提前注入；mode_test_service 复用 _set_env 一并生效。**Why:** 用户 llm_provider=custom(x666.me) + image_provider=sensenova 组合下，配置里已填的 key 被忽略导致 Step 2 生图 FATAL，报错文案误导用户重填。**How to apply:** 后续新增依赖 SENSENOVA_API_KEY 的功能直接读 env 即可，不要再把注入条件绑到 llm_provider；Web 运行中改 image_provider 无需重启服务（_set_env 每次运行都会重设 env）。
- [2026-08-31 00:02:02] [2026-08-31] 临时 .cmd/.py 验证脚本里 python -c 的 print() 勿回显 Windows 文件路径——路径中 \2026、\tTemp 等片段被 Python 解析成转义序列（\202→八进制 \x82），除 SyntaxWarning 外还会在 GBK 控制台触发 UnicodeEncodeError 直接崩溃退出。**Why:** 2026-08-31 py_compile 批量校验 .cmd 实测，print('OK %~1') 带路径即崩，修正为只 print('OK') 后通过。**How to apply:** py_compile.compile(r'%~1') 用 raw string 是安全的；脚本内需要输出标识时用序号或不含反斜杠的文件名，不要拼完整路径。
- [2026-08-31 20:06:50] httpx/starlette TestClient 会客户端规范化 URL 点段：GET 路径含 `..%2F`、`%2e%2e%2f` 等编码穿越序列时，httpx 在发送前解码并合并点段（请求被改写成别的路由，如 /script.json 返回 404），导致服务端 400 断言假失败。**Why:** 2026-08-31 缩略图白名单冒烟测试连续两次踩坑。**How to apply:** 测 FastAPI 路径穿越/文件名白名单防护时，直接 `asyncio.run(handler(name=..., filename=...))` 调 async 路由 handler 断言响应码，HTTP 层点段用例不可靠；POST body 传入的非法值不受影响可正常走 HTTP 断言。
- [2026-09-01 22:22:05] [2026-09-01, commit 7d6f9a0] Windows 子进程 stdin JSON 传参必须 ASCII 免疫：父进程 Popen(encoding='utf-8') 写 UTF-8 字节，子进程管道 stdin 默认按 locale（GBK）解码——中文变乱码、emoji 变 \udcXX 代理转义，路径类参数直接 FileNotFoundError。**Why:** 缩略图重生成子进程首版 ensure_ascii=False，run 名「【職場英文？…✨️｜Meeting」被解码成乱码路径，冒烟测试用 ASCII 目录名未暴露。**How to apply:** 跨进程传 JSON 的固定模式 = 父 json.dumps() 保持默认 ensure_ascii=True（payload 全 ASCII，任何编码解码结果一致）+ 子进程开头 sys.stdin.reconfigure(encoding='utf-8', errors='replace') 双保险；测试跨进程编码时必须用真实含中文/emoji 的目录名，ASCII 冒烟测不出。
- [2026-09-02 11:48:25] ffmpeg filter_complex 里未被显式标注输出的链（如 `...amix=...,volume=2.0` 无尾标签）不能被 `-map [a]` 引用——报 "Output with label 'a' does not exist in any defined filter graph"；链尾必须写 `volume=2.0[a]`。**Why:** 移植 BGM 参考实现时踩坑：参考项目混音频用默认流选择（无 -map、输出 mp3）所以没暴露此问题，一改成 `-map 0:v -map [a]` 的视频版就全部失败静默走 pydub 回退（侧链压缩失效）。**How to apply:** 凡 filter_complex + 显式 -map 组合，每个被 map 的输出流必须有方括号标签；冒烟测试要检查日志里没有"回退到 pydub"字样才算测到主路径。
- [2026-09-03 02:05:03] pose 图集人物截断修复定为纯 prompt 方案（commit 9388200）：3 处生成 prompt 的 'half-body close-up' 改为 'medium waist-up shot' + 显式完整性约束（格内留白/双肩完整/手臂手部不出格边），覆盖 image_gen.py 图集与 characters.py 图集+char_scene.png。**Why:** 用户在「prompt+自动重试 / prompt+仅告警 / 仅 prompt」三选项中明确选仅 prompt（零额外积分）；char_scene 一并纳入是用户选择。**How to apply:** 后续若用户再报截断，先提议贴边检测+重试方案并说明积分代价，勿擅自加检测/重试逻辑；生成参数 is_segmentation/prompt_extend/尺寸映射勿动。

### Project
- [2026-08-27 00:42:36] MOSS-TTS-Nano 引擎集成在 colab_listening_b_web 项目。模型路径：H:\models\MOSS-TTS-Nano-Model (checkpoint), H:\models\MOSS-Audio-Tokenizer-Nano (tokenizer), H:\models\MOSS-TTS-Nano (repo,含 moss_tts_nano_runtime.py)。API 入口：NanoTTSService.synthesize(text, voice, mode="voice_clone", output_audio_path)。16 个内置预设音色 (Ava/Adam/Bella 等)。**Why:** 用户已安装模型，需集成为第三 TTS 引擎选项。**How to apply:** moss_tts_engine.py 中 _get_service 必须在 import NanoTTSService 前先 import torchaudio_sf_patch（修复 Windows torchcodec DLL 缺失），否则合成报错 "TorchCodec is required"。CPU 生成一句英文约 15s，中文约 9s。2026-08-27 质量增强关键事实：①MOSS 会生成 1-3s 病态停顿（句中/句尾死寂），_clean_sentence_audio 用 20ms 帧级 RMS<0.008 检测压缩（句中>0.6s→0.25s，句首→30ms，句尾→80ms），时长校验必须在压缩之后做；②a.m./Dr./Mr. 等缩写句点不能触发分句（正则 lookbehind），过短碎片送模型会呓语；③中文台词默认中文预设音色 Junhao(男)/Xiaoyu(女)，英文参考音说中文带口音。
- [2026-08-27 01:48:36] [2026-08-27] 视频结构模式仅保留4种：original / original_static / original_cutout / quest。quest_v2 与 shorts 已于 2026-08-27 应用户要求彻底删除（连 structures.py/audio_envelope.py 一起清掉），旧结构名 image/static/checkpoint 迁移垫片也已移除。**Why:** 用户原话"其他的模式删除，相关代码脚本全部清理干净，不要有一点残留"。**How to apply:** 后续会话不要再引用或恢复 quest_v2/shorts/唇同步管线；若见到含这些名字的历史产物/checkpoint 属已废弃数据；新增模式建议需先向用户确认。
- [2026-08-27 02:03:12] [2026-08-27 02:02] original_cutout 开头/结尾已对齐 quest 主持人形式（commit 302fdb2）：timeline.rewrite_title_card_as_host_segments 移除标题卡并插入 welcome+hook_intro（speaker=host），outro 也为主持人出镜定格动画（演播室 host_bg.png，复用 quest._render_sm_segment）；TTS 旁白键变为 welcome/hook/outro + practice_intro（listening 脚本新增 welcome_en/welcome_zh 字段）；新配置「主持人形象绑定」(--host-character, char_a/char_b) 绑定时复用该角色姿势图集与音色、不再生成独立 host 图集。**Why:** 用户要求 ch2/ch3 不动、开头结尾换成 Quest 主持人形式并可绑定角色形象。**How to apply:** 改 cutout 时间轴时勿重建 title_card；resume 校验按 narration_names 结构清单在 image_gen.check_step2_resume。
- [2026-08-27 19:59:31] [2026-08-27] 密钥泄露后续（2026-08-27 三批修复中已将 configs/{default,mode_*,ori,quest,test}.json git rm --cached + gitignore，本地文件保留）：Sens eNova/OpenAI API key 与 MCP token 仍留在 git 历史与 GitHub 远程，用户需自行到对应平台轮换凭据。**Why:** untrack 不能清除历史。**How to apply:** 用户确认已轮换前，涉及这些密钥的操作注意失效风险；轮换后提醒更新本地 default.json。
- [2026-08-27 21:03:17] [2026-08-27] Ch3 跟读参数化与角色复用修复（commit 54187fe）。①timeline.build_listening_timeline 新增 en_repeats/zh_repeats（0-10，可全 0→跳过 practice_intro），段序=EN×N+sil → ZH×M+sil，废弃旧"中文后再播一次英文"尾播；②三个 compose 函数新增 ch3_zh_always kwarg：为 True 时 en_{i}.png 直接渲染含中文（内容同 zh 帧），段内文件选择逻辑零改动；③配置键 ch3_en_repeats/ch3_zh_repeats/ch3_zh_always(默认True) 在 PARAM_SPEC video 组，quest 模式 config.html 隐藏、间隔沿用 practice_duration；④build_cli_args 缺失键 ch3_zh_always 必须 get(k, True)——否则直传 dict 时默认变关闭。**Why:** 用户确认 EN×N→ZH×M 结构与开关默认开。**How to apply:** 后续调整 Ch3 结构只改 timeline.py 双循环；中文常显走渲染层 flag。
- [2026-08-27 22:28:22] [2026-08-27 22:28] FastAPI 路由遮蔽陷阱（main.py /api/scripts）：新增静态子路径 GET（如 /api/scripts/batch_status）必须注册在 `/api/scripts/{sid}` 之前，否则被当作 sid 匹配返回该 handler 的 404 JSON（签名：返回 {"error":"脚本不存在"} 而非预期数据）。**Why:** 实测 batch_status 曾返回 404，移动注册顺序后修复；同文件 form_options 因注册在前而幸免。**How to apply:** 往已有 {param} 通配路由的路径组里加静态端点时，一律插到通配路由定义之前，并在代码处留注释（main.py 已留）。
- [2026-08-27 22:54:45] [2026-08-27 22:54] 行级 LLM prompt 字段已按模式裁剪（commit c2c8b0d）：original 只生成 video_prompt、original_static 只生成 image_prompt、original_cutout 全不生成；poses 字段彻底废弃（管线从未消费，唯一读取者 generate_pose_images 已删除）。quality_gate_listening.PROMPT_FIELDS 为矩阵权威定义，_resolve_structure 显式参数 > script.structure > original 兜底；generate_listening_script/run_listening_qa/run_listening_quality_gate 均新增 structure 参数（未知值回退 original）。**Why:** 用户确认"全部裁剪"方案，省数千 token/降低 JSON 截断风险。**How to apply:** 后续新增模式需同步更新 PROMPT_FIELDS、llm_client 三个 _prompt 辅助函数、script_library 批量生成调用；不要再把 poses/image_prompt(除static)/video_prompt(除original) 当必需字段加回；旧脚本兼容靠 .get() 兜底不受影响。
- [2026-08-28 00:37:18] 本仓库常有多个并行会话同时改代码：2026-08-28 本会话改的 pipeline.py 被 another 会话的 commit a9e829f（00:29:36，做 ch3_zh_repeats include_zh）顺带卷入提交，message 与实际内容部分不符。**Why:** 用户同时开多个 Codely 会话在同一 worktree 工作，git add 整文件会扫走别人的未提交改动。**How to apply:** 提交前用 git status/diff 核对每个改动文件是否真是自己改的，只 add 自己明确改过的文件；发现工作区出现非自己改动的内容不要回退、不要提交，向用户说明即可。
- [2026-08-28 00:45:08] [2026-08-28] YouTube 元数据规则定型（commit dd9c4bb）：①章节硬规则=首章必须 00:00 且相邻 ≥10s（违反则 YouTube 禁用全部章节）——save_youtube_metadata 用候选 marks+10s 过滤实现，5s title_card 永不成为章节、dialogue 钳到 00:00 兜片头、quest hook_intro 被过滤属预期；②用户决策：youtube_title 目标 55-95 字符（gate (40,95)、保存 >100 截断），描述尾部 hashtag 保持全部追加、不去重不限量；③thumbnail_prompt 死字段已彻底移除（同 poses 类别），不要再加回 prompt/流程；④新增 POST /api/runs/{name}/yt_meta_refresh 从 script.json+meta.json 秒级重生成 youtube_metadata.json。**Why:** quest/cutout 原章节缺 00:00 会被 YouTube 全禁；80-150 字符标题上传被截断；用户确认裁剪与"保持全部追加"方案。**How to apply:** 改 timeline 结构/章节 label 时保持两条章节硬规则；hashtag 策略不要再"优化"成限量；章节 label 无代码消费方依赖，可改文案。
- [2026-08-28 12:42:01] [2026-08-28 12:45] youtube_metadata.json 支持多样式选项（commit fa26c3f）：顶层字段不变，新增 title_options 数组（reference_v1 参考频道同款繁中标题/简介，含時間軸繁中章节注入+链接配置区）。用户决策：同款字段 youtube_title_ref/description_ref 由 Step 0 LLM 生成（不用纯模板）；gallery 页要选项卡片+复制按钮；链接区留可编辑配置（reference_v1.py 顶部 PLAYLISTS/RELATED_VIDEOS）。**Why:** 用户要为三个 original* 模式多一套参考频道风格供上传多选一，且以后会增加更多参考样式。**How to apply:** 扩展新样式 = pipeline/yt_meta_styles/ 丢新 .py（STYLE_ID/LABEL/STRUCTURES/build 契约，docstring 有说明）；注意 save_youtube_metadata 的 marks 元素是英文 label 非 seg_type；旧脚本无 ref 字段自动跳过该样式，勿当作 bug；quest 不生成选项。
- [2026-08-28 13:06:06] [2026-08-28 13:10] QA 循环语义定型（commit b8b8481）：配置 quest_qa_rounds（现名「QA 轮数（全模式）」）驱动全部 4 模式——同时写 QUEST_QA_MAX_ROUNDS（quest）与 LISTENING_QA_MAX_ROUNDS（original*；llm_review._env_int 必须走 llm_client._env_get，直接读 os.environ 会让批量生成的线程局部 override 失效）；循环=跑满设置轮数且每轮必跑双评审（story+language），轮满仍有 error 继续修到 0（硬上限 max(10, N)，防死循环），有 error 但无可行动 patch 提前 break（空转保护），0=关闭需 `is not None and != ""` 判断才写入 env。**Why:** 用户明确选择"无 error 也跑满设置轮数（深度审查，不提前结束）"+"硬上限 10 轮"（否决了无限循环和连续无改善即停），不要当 bug"优化"回旧的超限 best-effort 行为。**How to apply:** 改 QA 循环时保持 pipeline/llm_review.py 与 pipeline/quest/llm_client_quest.py Phase E 两处语义一致。
- [2026-08-28 23:06:52] [2026-08-28 23:10] 运行目录布局改为 output/{mode}/{run_name}/（4 模式各一文件夹），回收站为 output/_recycle_bin/（兼容旧 .recycle_bin）。**Why:** 用户要求不同模式分开保存。**How to apply:** 路径解析统一走 config_manager.find_run_dir/iter_run_dirs（兼容旧扁平布局）；运行名跨模式可能重名，runs/gallery 相关 API 支持 ?mode= 查询参数消歧；checkpoint.load_checkpoint 扫描时跳过回收站文件夹；commit a175090 附带提交了并行会话的 run_mutex.py（pipeline_service import 依赖，非本会话功能）。
- [2026-08-28 23:09:22] [2026-08-28 23:20] 「⚡ 模式效果测试」页已上线（/mode-test，commit 0c46d1c）：每模式一次性生成迷你素材（original* 4行/quest 8行）存 output/.mode_test/assets/{mode}/（active.json 指针 + test_assets.json 清单），之后经 _step5_compose 零积分本地合成测试视频；与主 pipeline 经 app/run_mutex.py 全局互斥。**Why:** 用户要随时零消耗对比各模式效果。**How to apply:** .mode_test 是隐藏测试目录——运行列表/iter_run_dirs 靠点前缀自动排除，勿"修复"成可见或当垃圾清理；素材合成结构参数读 manifest 快照（timeline 已按快照构建），勿改读当前配置；素材生成中断靠 checkpoint 续传，成功后服务会删 checkpoint。
- [2026-08-29 01:19:13] [2026-08-29 01:20] 生图 Provider 双通道上线（commit af59fd2）：image_provider 配置（mcp 默认 | sensenova=U1.5 Lite，API 计费复用 SENSENOVA_API_KEY）。用户决策：新增选项不替换 MCP、watermark=false（公测免费去水印）。关键实现事实：①U1.5 两个端点均为同步 JSON 接口——/v1/images/generations 纯文生图、/v1/images/edits 走 images[].image_url（公网 URL 或 data:image/*;base64，不支持 multipart/裸 base64/chat completions）；②返回 URL 仅 24h 有效→sensenova 路径统一"下载落盘→reupload_for_cdn 换 TOS 永久 URL"再进 image_urls（Seedance/resume 安全）；③U1.5 无 is_segmentation，quest/cutout 姿势图集靠 stop_motion 白度抠图 fallback；④图集类 prompt 必须 prompt_extend=False 防 "4x2/2x2 grid" 布局被润色改写；⑤缩略图 U1.5 失败直落 Pillow 不回退 MCP（防意外耗积分）。**Why:** MCP 积分易耗尽，SenseNova key 已有。**How to apply:** 尺寸映射在 pipeline/sensenova_image.py SIZE_MAP（16:9→2720x1536，图集 4096x2720/4096x2304）；改生图相关逻辑时两个 provider 分支都要维护。
- [2026-08-29 23:03:35] [2026-08-29 23:05] 句子限长约束链上线：配置 max_line_words（content 组，默认 10，clamp [4,20]，空/0→10）保证字幕最多两行。env 双通道 LISTENING_MAX_LINE_WORDS（original*）+ QUEST_MAX_LINE_WORDS（quest），注入点 pipeline_service._set_env / script_library 批量 override（线程局部，读端必须经 llm_client.resolve_max_line_words 的 _env_get）/ pipeline.py main()（CLI --max-line-words）。约束三层：①LLM prompt（对话行 HARD LIMIT + 旁白字段 welcome_en/story_hook/outro/practice_intro_en、quest hook_intro_en/outro 每句 ≤cap，CEFR 词数区间 hi=min(hi,cap)）；②质检门禁超长 warning→error 触发 QA 修复（quality_gate_listening._resolve_max_line_words / quest 同构）；③渲染兜底 media_utils._split_overlong_entries（burn_subtitles 内按实际字号测量，EN >2 行字幕条按词边界拆条+ZH 按字符比例+时长按字符占比，覆盖旧脚本/脚本库/QA 残留）。**Why:** 用户实测 15 词句字幕折 3 行影响学习。**How to apply:** 旁白超长是 warning 不是 error（旁白无行号不可 patch，error 会误触 QA 空转保护提前终止循环，勿"修复"成 error）；改字幕字号时兜底自动适配；quest _META_TARGETS 总词数目标（70-110/80-110）与每句 ≤cap 共存。
- [2026-08-30 01:04:31] [2026-08-30] 本机网络：huggingface.co 与 hf-mirror.com 均 TCP 不通，ModelScope（modelscope.cn）可达。**Why:** 下载模型权重必须优先找 ModelScope 镜像（AI-ModelScope/* 社区镜像或官方中文仓库）。**How to apply:** HF 直链失败时改用 https://www.modelscope.cn/models/{ns}/{name}/resolve/{rev}/{file}。
- [2026-08-30 21:57:39] [2026-08-30] 用户清单来源项目=Timeline Studio（github.com/MartinDelophy/ai-video-editor，MIT，浏览器 WebGPU 视频编辑器）；其 ONNX 模型镜像仓 martindelophy/timeline-studio-onnx-models（ModelScope+HF 双托管，revision a201b681）含 JoyVASA+LivePortrait 全部权重，SHA256 清单在仓库 src/config/{joyVasa,livePortrait}.js。**Why:** 本地 AI 能力的模型与推理逻辑移植自此项目（joyvasa.worker.js + liveportrait.worker.js）；其中 digital_human 数字人引擎已于 2026-08-30 应用户要求整体删除（代码/配置/UI 无残留，H:\models\digital_human 权重仍在但已无消费方）。**How to apply:** MODNet 抠图与 AI 超分仍在本仓库使用；LivePortrait 用纯 ONNX landmark 检测（无需 dlib/insightface，避开 Windows 编译坑）的结论仍有效。

- [2026-08-30 21:57:55] [2026-08-30] 本地 AI 能力上线（commit 7a2cf01/fb1565a/ea591ed）：①抠图 matting_engine=auto/modnet/white_threshold（MODNet ONNX H:\models\modnet，stop_motion.remove_bg 三分支，白度法保留）；②4K upscale_engine=ffmpeg/ai 默认 ffmpeg（realesr-animevideov3 torch CUDA fp16 ~110ms/帧，sr_upscale.py rawvideo 双管道流式，权重 H:\models\upscaling）；③digital_human 数字人（animation=digital_human，JoyVASA+LivePortrait）已于 2026-08-30 应用户要求整体删除（pipeline/digital_human.py、dh_quality/dh_neural_fps 参数、portrait 生图、config.html 显隐逻辑全清）。**Why:** 用户要求「保留原来的方法功能，可以在配置里设置用哪个和用不用」，随后又要求删除数字人。**How to apply:** 抠图/超分仍走 PARAM_SPEC→build_cli_args/_build_args→env/args 三层接线；不要把回退路径当 bug 删掉（权重缺失回退是设计行为）；不要再恢复 digital_human 相关代码或参数。

- [2026-08-30 21:58:08] [2026-08-30] animation 参数四模式真实语义（勿按旧文档理解）：original_cutout 下 none/landing/stop_motion 三值渲染结果完全相同（都走双人定格动画，合成器无 animation 分支——原 digital_human 分支已于同日删除）；original_static 管线强制 animation="none"（pipeline.py main + _build_args 双处，landing/stop_motion 实为死值）。**Why:** CODELY.md 架构段旧文档仍写 static「支持 none/landing/stop_motion」与实际不符。**How to apply:** 给 static 恢复动画需先解除两处强制；cutout 动画选项文案已如实标注等效（commit 61efb31）。

- [2026-08-30 13:33:13] [2026-08-30 13:35] moss-tts「视频第一句杂音」根因与修复（commit 848c714）：original_cutout 的 welcome.mp3 恒为 MOSS 引擎冷启动后第一次推理，超短句（如 "Welcome back!" 2词）首采样易产生满能量爆音/含混——历史 5 次 cutout 运行 4 次 welcome 首句呈 [≤12%,>75%,>75%,>75%]（帧RMS占p95%）无起音包络形态，近静音/时长校验均拦不住，成片忠实搬运即杂音。**Why:** 段级 afade 仅 50ms 压不住；下游 concat/loudnorm 链路经包络相关 0.975 验证无污染。**How to apply:** 修复在引擎层：①_get_service 后 _warm_up() 丢弃式预热（~2.7s，真实首句不再冷启动）；②_synthesize_sentence Validation 1b 用 _has_leading_burst（首4帧无起音形态检测，末次接受仅告警）+ 淡入 10ms→25ms；quest/original 首句音频结构不同（quest welcome=qwen 时无此问题；original 首句 dialogue_0 时模型已热）但切 moss 引擎同样受保护。检测器签名可复用：head[0]≤0.15 且 head[1..3]≥0.75（169 文件 0 误报，qwen 样本全负）。
- [2026-08-30 20:54:52] [2026-08-30 21:00] 缩略图「仍走 MCP 耗积分」根因与修复（commit b6cd1e2）：dashboard.html 的 MODE_CONFIGS 是页面加载时的 Jinja 配置快照，startPipeline() 直接把快照 POST /api/run/start，后端 data.config 优先——配置页刚保存的 image_provider=sensenova 被旧快照的 mcp 覆盖，缩略图/生图全走 MCP。**Why:** 用户实测缩略图未跟随「🎨MCP/图片」生图 Provider；thumbnail_gen.py 的 sensenova 分支本身无 bug。**How to apply:** ①startPipeline 现在先 fetch /api/config/all 刷新 MODE_CONFIGS 再合并快捷覆盖项（_doStartPipeline）——后续给 dashboard 加任何"启动前快捷覆盖"都必须走这个刷新流程，勿直接用页面快照；②配置新增 no_thumbnail checkbox（mcp 组，默认 False）：勾选=Step 4.5 只生成 youtube_metadata.json 不生成 thumbnail.jpg，接线=PARAM_SPEC/build_cli_args --no-thumbnail/_build_args.no_thumbnail，_step45_thumbnail 的 resume 判定是 `no_thumbnail or exists(thumb_path)`；③当前 4 个 mode_*.json 的 image_provider 都是 mcp（2026-08-30 20:47 状态），用户要省积分需自行切 sensenova。
- [2026-08-30 23:44:06] [2026-08-30] MOSS-TTS 采样参数已全面暴露（commit 648f974）：配置页 TTS 组新增 moss_tts_top_p/top_k/rep_penalty/text_temperature/greedy（默认=原硬编码值 0.95/25/1.2/1.0/False，零行为变化），三层接线 build_cli_args/pipeline.py env/pipeline_service + 音色试听端点同步传参 + tts_pipeline extra_sig 缓存签名。**Why:** 用户要求"暴露更多采样参数"用于音质/音色稳定微调。**How to apply:** ①贪心(moss_tts_greedy)=do_sample=False 时 HF generate 忽略温度/top_p/top_k，但 NanoTTSService.synthesize 恒转发全部采样参数给 model.inference——贪心模式可能出 transformers UserWarning 日志噪音，属预期勿当 bug 修；②改这些参数自动清 TTS 缓存（extra_sig 含 MOSS_TTS_TOP_P/TOP_K/REP_PENALTY/TEXT_TEMPERATURE/GREEDY）；③MOSS_TTS_GREEDY env 真值解析 "1"/"true"/"yes"/"on"。
- [2026-08-30 23:57:32] [2026-08-30 23:57] 定格动画对白段「无声+无人物」根因与修复（commit 5e28bc4）：workers>1 时多线程共享 %TEMP%/sm_frames_*/cutout_*.png 姿势缓存，读线程命中写线程写到一半的 PNG（读取分支无 try 保护）→ 异常逃逸 → 该段被「静态背景+anullsrc 静音」占位。诊断特征链：①成片该段 -91dB 数字静音且画面纯背景无人物；②SRT/音频时长正常（TTS 文件完好）；③%TEMP% 残留空 frames 目录（正常完成/ffmpeg 失败都会 rmtree，只有 Python 异常逃逸才残留）。**Why:** run「今年我需要報稅嗎」dialogue_1 实锤，时间戳与并发写缓存完全吻合。**How to apply:** 已修（原子写 tmp+os.replace、坏缓存自愈、外层 try/finally 清理、失败重试一次、回退/占位段保留有效音频、_resolve_seg_audio 共用）；再遇「某句无声无人物」先查 %TEMP%\sm_frames_{run名} 是否残留空段目录即可定性；修复后重新 compose 同一 run 可重建该段（素材齐全零积分）。
- [2026-08-31 00:02:02] [2026-08-31, commit 7c29edb] 角色音色绑定参数 character_moss_voices / character_kokoro_voices 有意不加 PARAM_SPEC（沿用 moss 先例）：属控制台角色区 UI 管理的参数，走 /api/config/save 任意键持久化 + load_mode_config merge 保留，config.html 不渲染 character_* 绑定。**Why:** moss 绑定上线即此模式，kokoro 第三引擎对称沿用；防未来会话把 PARAM_SPEC 里的"缺失"当 bug 补。**How to apply:** 后续新增引擎绑定参数（character_xxx_voices）照抄此模式，勿主动加 PARAM_SPEC/补 config.html 渲染；kokoro 绑定链 = script.json {key}_kokoro_voice（tts_engine.build_voice_map 优先级1，高于性别自动）+ narration_kokoro_voice（tts_pipeline 按引擎分流，qwen/moss 仍读 narration_qwen_speaker）。
- [2026-08-31 10:38:51] [2026-08-31] 「🎵 Kokoro 音色」管理页上线（commit 3b3fce1 + 试听修复 d58fd8d，与 voices/moss_voices 三页对称）：/kokoro_voices 路由+模板+侧边栏；默认音色配置 configs/kokoro_voice_config.json 由 Web（GET/PUT /api/kokoro_voices/defaults）与 pipeline tts_engine._load_kokoro_voice_config 共享，缺文件/缺键回退内置 am_adam/af_sarah/af_sky（零行为变化）；tts_engine._download_voice 下载源顺序 ModelScope(AI-ModelScope/Kokoro-82M)→hf-mirror→huggingface。28 个英文音色 .pt 已全部下载（SHA256 校验过），get_all_kokoro_voices 28/28 cached。**关键坑（d58fd8d）**：KPipeline load_single_voice 走 hf_hub_download 只认标准 HF snapshot 目录布局，非 snapshot 位置（如顶层 voices/）的 .pt 对 _find_voice_in_cache(os.walk)可见→cached=true 但 hf_hub_download 找不到→合成空音频，异常被 _kokoro_synth_chunk._try 吞掉只报 "produced no audio"；修复=synth_english 入口把 voice 名经 _resolve_voice 解析为本地 .pt 路径直传（load_single_voice 支持 '*.pt' 路径模式，_synth_chinese_kokoro 先例），绕过 hf_hub_download 零网络请求（0.4s/句）。**How to apply:** 新增 Kokoro 音色只需扩 KOKORO_VOICES 清单；遇 "produced no audio" 先查网络重试日志定位真因（_try 吞异常）；不要再把「cached=true 但合成失败」当缓存问题修。
- [2026-08-31 13:31:22] [2026-08-31] P0 架构拆分完成（6 commits 272734e..90e0aff）：app/main.py 4218→95 行纯装配层（16 个 include_router + startup），路由全部迁入 app/routers/（pages 14页面/health/config/run/mode_test/topics/scripts/styles/subtitle_styles/runs/mcp_tokens/characters/voices_qwen/voices_kokoro/voices_moss/ai_test），共享基础设施在 app/{paths,templating,sse,topics_io,tts_state,library_io}.py，pipeline_runner.py 已删除。**Why:** 153+ 路由平铺 + 16 处 sys.path hack 不可维护。**How to apply:** ①include 顺序=原 banner 顺序，新增 router 时保持领域分组插入；②路由遮蔽约束现存在于 routers/scripts.py（batch_status 在 {sid} 前）与 routers/subtitle_styles.py（preview/current 在 preview/{style_id} 前）；③bootstrap 双保险：config_manager 模块级 + routers/__init__ 调 paths.ensure_pipeline_on_path()；④三语音 router 共享 tts_state.TTS_SYNTH_LOCK，新增引擎照抄 voices_kokoro.py 模式；⑤FastAPI 0.141 _IncludedRouter 懒包装——枚举路由表需展开 original_router.routes（旧 route_dump 单行脚本会 AttributeError）；⑥startup 设计音色冻结经 voices_qwen.auto_freeze_pending_designed_voices 公开函数调用；⑦CODELY.md 架构段仍写"pipeline_runner.py 兼容包装/路由全在 main.py"，因并行会话占用未更新，下次会话应订正。
- [2026-08-31 13:49:41] [2026-08-31] TTS 语速参数重构（commit 31e7f92）：tts_rate 单参数拆为 tts_rate_en/zh/narration 三分项，模式默认值唯一来源=TTS_RATE_DEFAULTS 表 + resolve_tts_rate()（优先级：分项覆盖 > 旧全局 tts_rate > 模式默认）；pipeline.py 与 pipeline_service.py 两处 original_cutout 强制 0% 特判已删除。**Why:** 用户认为一个 tts_rate 一刀切覆盖 4 种内容太混乱；行为保持零变化。**How to apply:** ①默认表关键事实——cutout 中文/旁白实际是 0%（旧全局强制外溢，非 -10%），quest 中文不生成，调整默认只改表；②configs/*.json 已迁移（tts_rate 真值→tts_rate_en，cutout 存的是字符串"0"）；③缓存 .tts_meta.json 签名现为解析后 rates dict，旧格式 meta 首跑清一次本地 TTS 缓存属预期；④新增 TTS 内容类型（如词汇）照 resolve_tts_rate(kind, structure) 模式扩展。
- [2026-09-02 12:46:16] [2026-09-02 订正] Web 服务（端口 8765）由 run.bat 启动、uvicorn 带 --reload：reloader 父进程 StartTime 停在服务首次启动时刻（08-28 12:46），代码变更时 worker 子进程自动重生（查法：Get-CimInstance Win32_Process -Filter "ParentProcessId=<父PID>" 看 CreationDate）——父进程 StartTime 旧 ≠ 代码旧。**Why:** 2026-09-02 排查「未配置 LLM API Key」时曾被父进程 5 天前的 StartTime 误导为旧代码，实测 worker 当时刚重生、代码是新的；08-31 的"长期持旧代码"结论应据此重审。**How to apply:** 诊断"代码已修但行为未变"时先查 worker 子进程 CreationDate（非监听父 PID StartTime）；--reload 正常工作时改完代码无需提醒重启，仅当服务被手动无 --reload 启动才需重启。

- [2026-08-31 14:19:20] [2026-08-31] 字幕分句同步机制与诊断方法定型（commit 37eb936）：burn_subtitles._split_subtitles 按句拆分 EN/ZH 配对后，_align_entries_to_pauses 把分句边界吸附到实测静音终点（=下一句语音起点）；seg_pauses 必须排除段首 lead-in 静音（ss > 段首+0.15，其终点=首句自身语音起点，误吸附会让字幕比音频提前一句、首句配对只在 0.3-0.4s 静音里闪现）。**诊断套路：** ①ffmpeg 按 0.1s 步进抽帧看字幕实际切换区间；②silencedetect 对 dialogue_N.mp3/无字幕成片测语音包络比对；③run 目录的 final_no_sub.mp4 在 videos/ 子目录（不在根目录）。**How to apply:** 再遇"字幕与语音不同步"先用 ①+②定位切换点与语音起点的偏差，勿凭肉眼描述下结论；run 目录名含中文+emoji（U+FE0F），cmd /c 与后台 shell 会把它乱码成" No such file"——分析前先 Copy-Item 到 %TEMP% ASCII 路径（后台 shell 连 Copy-Item 都可能失败，前台执行）。
- [2026-08-31 16:46:17] [2026-08-31] 段拼接音画累积漂移根因与修复（commit 6205e4a）：concat demuxer -c copy 按段内 AAC 原始轨道时长（含 priming，edit list 不参与）推进下一段起点，每段 +40ms，304 段累积 +2.2s——Ch2 对话字幕（按 timeline 计划时间烧录）相对音频越来越提前；修复=concat_segments 改三段式：视频 concat demuxer -c:v copy + 音频逐段解码成 s16le PCM 按 ffprobe 容器时长字节级裁尾后拼接、一次 aac 编码 + remux。**关键坑**：仅解码重编码不够——edit list 只裁起点 priming 不裁尾部，解码保留编码器补齐到 AAC 帧边界的静音（平均 ~11ms/段，120 段实测 +1.3s）；atrim=end_sample 是整帧粒度（1024 样本）裁不掉尾部，必须 PCM 字节层裁。配套：定格动画段编码 `-r 25` 输出旗标改 `-vf fps=25`（floor→ceil 帧数，段视频覆盖满时间槽）。**Why:** 用户报最新 cutout 视频字幕越到后面越快，宽范围互相关实测 lag 从 +0.10s 线性涨到 +2.24s（corr 0.96~1.00）。**How to apply:** 遇音画渐进漂移先测「音频实际落点 vs 计划边界」（互相关法，20ms 网格），恒定偏移=TTS 前导静音无害，线性增长=拼接推进 bug；诊断脚本思路：解整片 16k mono 包络 vs 逐句 mp3 包络滑窗互相关。已验证：4 段真实素材漂移 0.000s，120 段总长精确 60.000s、落点 corr=1.0000。run「重渲字幕」(recompose) 只重烧字幕不重建段，修不了已烘焙的漂移；坏旧 run 需整链重跑 Step5。
- [2026-08-31 17:25:35] [2026-08-31] Kokoro TTS 质量增强上线（commit c1989e2，tts_engine.py）：①_normalize_tts_text_en（$20→20 dollars、10%→10 percent、8:30→eight 30、2026→twenty twenty six）在 _apply_phonetic_fixes 之后、_clean_text_for_kokoro 之前执行（cleaning 的 [^\w\s.,!?\'] 会剥 $%: 丢语义词；字幕直读 script.json 不受影响，归一化仅 TTS 层）；②_split_sentences 移植 MOSS 缩写 lookbehind（Dr./Mr./a.m. 不再误切分）——该函数被 qwen_tts_engine.py import 复用，qwen 同步受益，改签名需同步两处；③多句合成插 0.12s 句间静音 _SENTENCE_GAP_SEC；④_normalize_wav 将 loudnorm 移到 PCM 域、MP3 有损编码 3次→1次（_loudnorm MP3 版保留给 edge-tts，moss/qwen 引擎未动）。**Why:** 对照社区 Kokoro 最佳实践补 4 个真实缺口，用户确认全做。**How to apply:** _SENTENCE_GAP_SEC=0.12 有意不暴露为配置项（内部韵律常量），勿主动加 PARAM_SPEC；改数字归一化规则只动 _normalize_tts_text_en；旧 run 缓存音频仍有效，新规则只影响新生成（清 run 的 audio 目录可重生）。
- [2026-08-31 20:13:08] [2026-08-31, commit fa26b80 + f550616] original_cutout Ch2 多场景 + host_bg 多样化上线：①listening prompt 新增 cutout 专属条件字段 host_bg_prompt（与主题呼应的演播室）+ scene_images（恰好 8 个 {"prompt","label"}，同 location 不同角度/细节），original/static 不生成（延续字段裁剪决策）；注意 _build_listening_prompt 的 style_section 原本对 cutout 从不输出（行级 prompt_fields 为空），现条件改为覆盖 host_bg_prompt/scene_images；②Step 2 cutout 分支追加 _generate_scene_atlas（quest 同款 2×2 atlas，8 场景=2 次调用），scene.png 主背景照旧生成（标题卡/练习区/兜底用）；③Step 5 cutout 分支收集 scene_bg_list（scene_N 缺失回退 scene_img，旧脚本无字段时行为同前）；④场景轮换 cadence 自适应 = max(1, 总对话段 // 背景数)，cutout（18 行×8 背景→每 2 行一换）与 quest（f550616 起同款，48 行×8→每 6 行一换、修 12 张图时后 2 张永不出现）两处公式一致。**Why:** 用户要求 Ch2 对话阶段尽量多点不同场景、host_bg 别每次都一样（原为 pipeline.py 硬编码 prompt），并确认 quest 也要同样效果。**How to apply:** cutout/quest 两处 cadence 公式改一处需同步另一处；不要把旧脚本无 scene_images/host_bg_prompt 当 bug（setdefault 兜底是有意设计）；质量门故意不加这两个字段的硬校验（scene_images 缺失只影响丰富度，加了 error 会触发 QA 空转保护）。

- [2026-08-31 20:06:50] [2026-08-31, commit 0315176] 运行历史「生成多一张缩略图」功能上线：POST /api/runs/{name}/thumbnail_regenerate 后台线程（照 recompose 模式 + run_mutex.try_acquire("thumbnail_regen")），复用 Step 4.5 thumbnail_gen.generate_thumbnail，输出 thumbnail_N.jpg 递增、旧图全保留；主图由 run_dir/thumb_main.txt 记录（GET /thumbnail 与卡片封面跟随，dashboard 复用同 URL 自动生效），runs.py _THUMB_NAME_RE 白名单 thumbnail(_\d+)?.jpg 防路径穿越；卡片「＋缩略图」按钮 +「N 张」角标 + thumbs-modal 弹窗（浏览全部/设为主图）。**Why:** 用户确认"旧图全保留 + 弹窗浏览 + 设为主图"三项需求。**How to apply:** regenerate 期间 dashboard 显示「运行中：缩略图重生成」属互斥预期；角色参考图 quest/cutout 取 images/pose_char_a_0.png、其余取 images/char_scene.png——sensenova 分支本地路径可直传（edit_image 内 _to_image_url 转 base64），mcp 分支必须 CDN URL（线程内 mcp_initialize + image_gen.reupload_for_cdn，上传失败退回无参考图生成是有意 fallback 勿当 bug 修）；真实 AI 生成链路未自动测（耗积分），由用户 UI 点按验证。
- [2026-08-31 20:13:21] [2026-08-31, commit f550616] 主持人演播室背景配置上线（quest+cutout）：①控制台快捷区「主持人演播室背景」下拉（data-qc-group="hostbg"，onQuickInput 任意键持久化 host_bg_source，不入 PARAM_SPEC），refreshHostBgOptions() 从 _libraryChars/_charSources 按 has_host_bg 动态填充，值格式 lib:<素材库ID> / run:<运行名>；②pipeline_service._apply_host_bg_binding 在 _run_inner 角色复用块后无条件调用（自守卫：空值/非 quest+cutout 结构 no-op），把源 host_bg.png 复制进运行 images/（dst 已存在跳过、源缺失日志回退自动生成、幂等 resume 亦生效）——generate_images 对已存在文件自动跳过生成，绑定零积分；③配置页 PARAM_SPEC host_bg_prompt（text，content 组，quest+cutout）固定描述覆盖，build_cli_args/--host-bg-prompt 与 _build_args 双通道；④优先级=绑定文件 > 固定 prompt > script.host_bg_prompt（LLM 每期）> 硬编码默认；⑤/api/character_sources 与 /api/character_library 各加 has_host_bg 字段。**Why:** 用户要求像 cutout 主持人形象绑定那样可固定演播室（频道视觉统一），同时保留固定 prompt 与每期自动三选一。**How to apply:** 改绑定需删运行目录中 host_bg.png（存在即跳过与角色复用语义一致）；mode_test 有意不接入（测试素材 host 段维持 scene_img 回退）；角色套装 _SET_FIELDS 不含 host_bg_source；素材库保存端点 quest 结构才复制 host_bg（cutout 保存不存，既有缺口未修，cutout 用户用 run: 绑定覆盖）；dashboard 若加其他"启动前快捷覆盖"仍须走 /api/config/all 刷新流程。
- [2026-08-31 21:17:38] [2026-08-31] 架构审查计划（Step Registry/PipelineContext/Pydantic Schema/Quest 流水线复用/Topic Brief 等）经核查约 1/3 前提过时：original 已有 run_listening_qa 多轮双评审+quality_gate 硬门禁；脚本库已有 score/verdict/local_issues；title_quote/youtube_title 已在 Step 0 前置；thumbnail concept 前置与 dd9c4bb「thumbnail_prompt 死字段勿加回」决策冲突。用户拍板：筛出的三项（json_store 原子写、run_state.json 落盘、Step Registry）全部暂缓不实施，方案仅存会话 plans 目录作参考。**Why:** 用户评估后决定暂不动架构。**How to apply:** 后续会话勿把该审查计划当已批准路线图执行；用户重提架构优化时可引用本次核查结论再确认。
- [2026-09-01 00:28:55] [2026-09-01] 人物素材库「AI 一键生成通用角色」上线（commit 049ac13）：POST /api/character_library/ai_generate {structure: quest|original} 起后台线程，resolve_provider(load_config())+urllib 同步调 LLM（不走 pipeline _chat，其 env 注入 Web 非运行态不可靠；解析复用 llm_client._extract_json），一次产 5 个日常场景通用角色**直接入库为仅描述卡**（无图），meta 新字段 role/scenarios(中文标签)/origin="ai"，ID=char_{毫秒+i}_{gender} 防批量撞号；GET ai_generate_status 单槽轮询 + 409 并发守卫；用户审核后点卡片🎨走原有 MCP 生图链路（结构在生成时选定）。**Why:** 用户要求描述先行、合适再生图、场景随卡保存显示。**How to apply:** 卡片 scenarios 徽章渲染在 characters.html badges 区；并行会话改动（CODELY.md/active_mode.json/qwen_voice_config.json）未卷入提交属预期。
- [2026-09-01 10:59:35] [2026-09-01] 人物素材库图集弹窗 + AI 生成维度筛选上线（commit 340663a）：①GET /api/character_library/{lib_id}/atlas 四级兜底 pose_atlas_{source_key}→glob pose_atlas_*→char_scene→thumb，卡片图/姿势缩略图点击 openAtlasViewer 弹窗（适应窗口/原始尺寸切换、ESC/背景关闭，modal 用 hidden↔flex 类切换）；②ai_generate 筛选维度 gender/age/skin/extra（勾选才传，空=随机 mix），gender 白名单校验、其余截断 60/300 字符，注入 prompt「USER CONSTRAINTS (highest priority)」块覆盖 mix genders/ages 要求。**Why:** 用户要求点图看完整 pose_atlas + 生成可控性别年龄肤色等。**How to apply:** 弹窗复用 _libraryChars 查找（openAtlasById）；新增维度照 _opt 局部函数模式加进 api_library_ai_generate 与 constraint_lines。
- [2026-09-01 11:09:38] [2026-09-01] 脚本库 AI 审查/修复已结构感知（commit c23f4bc）：app/script_library.py 新增 _line_prompt_field（original→video_prompt/static→image_prompt/cutout+quest→无）与 _patchable_line_keys（按结构裁剪 text/zh/speaker/phonetic/行级prompt，poses 已彻底移除），_apply_patch(script, patch, structure) 按结构取键；local_checks 行长阈值与 QA 门禁同源（resolve_max_line_words 按 structure 走 LISTENING/QUEST_MAX_LINE_WORDS，severity medium）+ 简体检测补 welcome_zh。**Why:** 修复前 original 脚本审查 img 恒空一致性维度失效、修复可盲改 video_prompt（Step3 真实消费有场景一致性风险）。**How to apply:** quality_gate_listening.PROMPT_FIELDS 或 quest 行字段（无 phonetic）变更时必须同步 script_library 这两个辅助函数；core 兼容性核查结论=脚本库生成→审查→修复→运行全链路与 4 模式匹配（签名/线程隔离/运行衔接均验证通过）。
- [2026-09-01 13:36:03] [2026-09-01, commit 018b7b0] 脚本库脚本×角色绑定性别相反自动交换 A/B 上线：pipeline_service._resolve_gender_conflicts 在脚本库 seeding 后、_reuse_characters 前调用（仅 script_id 新运行，resume 不触发），绑定性别取素材库 meta.json / 源运行 script（reuse 的 image/desc/voice 模式）；冲突且 A/B 互换可匹配时 _swap_script_characters 交换顶层 char_a_*/char_b_* 全字段 + 对白 speaker a↔b 并写运行副本（脚本库原稿不动），不可匹配打 [GenderSwap] 警告；quest char_c / cutout host 仅提示不交换；script_library._doc_meta 新增 genders 字段 + 脚本库卡片/控制台下拉 ♀♂ 徽章。**Why:** 行级 video_prompt/image_prompt 有意保持不动——画面人物随脚本性别、音色/图片槽位随绑定性别（音画一致），未来会话勿把"行级 prompt 性别与绑定不符"当 bug 修。**How to apply:** 改绑定/复用逻辑时保持调用顺序（gender 冲突解决必须先于 _reuse_characters 覆盖）；新增绑定来源需同步 _resolve_gender_conflicts 的 bound 收集。
- [2026-09-01 14:19:32] [2026-09-01] 三引擎（Qwen/MOSS/Kokoro）默认音色按模式分套 + 同性别错开上线（commit de4468e）：{qwen,moss,kokoro}_voice_config.json 新增顶层 modes（original/original_static/original_cutout/quest 各一节），键=default_male/female/host_male/host_female(+2/3)（host_male 同日追加 a7e98e0：主持人按 host_gender 取 host_male/host_female，三页标签已由「默认旁白女声」改「主持人默认音色（男/女）」，host_male 内置=各引擎第一男声零行为变化，可按模式覆盖避免男主持与男角色撞音色），moss/qwen 另有 default_*_zh(+zh2)；解析链 modes[mode]→平铺 legacy→内置默认（kokoro 无文件走内置 am_adam/af_sarah/af_sky/am_liam/af_nova/am_echo/af_jessica）；未绑定同性别角色按 char_a→char_b→char_c 依次取 1/2/3 默认（仅 quest 有 3 角色，缺键回退 X3→X2→X），host 不参与分组；_load_voice_config 必须 defaults.update(saved) 保留未知键（逐已知键回拷会丢 modes 节）；defaults PUT 签名={mode, values}，GET 返回补全的 4 模式 modes。**Why:** 用户要求不同模式下、不绑定角色、同性别角色能区分音色（第一/第二默认）。**How to apply:** 第三默认仅 quest 消费；qwen en 男预设仅 Ryan/Aiden，quest 三男内置回退同 Aiden、moss en 女仅 Ava/Bella（UI 可改绑自定义/设计音色）；kokoro 中文 edge-tts 不在配置范围；改 build_*_voice_map 签名需同步 tts_pipeline 传 _structure 与 gender_default_ranks rank。
- [2026-09-01 22:02:12] [2026-09-01, commit 3cf5414] 缩略图重生成改独立子进程，可与主 pipeline 并行：pipeline/thumbnail_regen_cli.py（stdin 收 UTF-8 JSON 参数规避 argv 中文/emoji run 名乱码、stdout PIPE 日志、退出码 0/1）+ app/thumbnail_regen_service.py 单槽管理（不获取 run_mutex；同 run 守卫：正在生成的 run 拒绝等 Step 4.5 自动出图；服务重启后 idle 视为结束防死轮询），GET /api/thumbnail_regen/status 增量轮询，pipeline_service 旧 regenerate_thumbnail/_thumbnail_regen_run 已删。**Why:** 旧线程实现与 pipeline 共享 PipelineService 单例/sys.stdout(_LineBuffer)/mcp_client 全局会话被迫 is_running+run_mutex 双重互斥，用户要求生成视频过程中也能生成缩略图。**How to apply:** 0315176 记忆中 run_mutex 互斥描述已过时（thumbnail_regen 不再是锁 owner，现 owner 仅 pipeline/mode_test:*/4k_gen）；「dashboard 显示运行中：缩略图重生成」不再出现；若要给 4K/recompose 做同样并行化，照抄此子进程模式（stdin JSON + PIPE 读线程 + 单槽）；mcp provider 下子进程与 pipeline 共用同一批 token 各自轮换属预期。
- [2026-09-01 22:38:10] [2026-09-01, commit 718fdae] MCP generate_image 高成本配置坑（缩略图静默落 Pillow 兜底根因）：provider=frontier（含 quality=high）等高级通道必须带 confirm_cost=true 才真正建任务，否则只返回积分确认提示（frontier ~50 积分/张）、parse_task_id 为空、thumbnail_gen 的 if task_id 块被静默跳过直接落 Pillow——现已改 seedream 普通通道（用户选「跟随当前配置」）+ task_id 为空时打印原始响应不静默；端到端实测 seedream 90s 出图。同 commit 上线缩略图删除：POST /api/runs/{name}/thumbnail_delete（白名单/删主图清 thumb_main.txt/生成中同名 409），弹窗卡片删除按钮 + 「N张」角标同步。**Why:** 用户报「总是生成兜底缩略图不生成 MCP 的」+ 要求删除不喜欢的缩略图。**How to apply:** 今后任何 call_tool("generate_image") 换 provider 前先查高成本规则（非默认 provider 都要 confirm_cost）；若要给缩略图换回 frontier 高质量，加 confirm_cost: true 并告知用户每张 ~50 积分。
- [2026-09-02 01:11:45] [2026-09-01, commits 20b417b + 74cd765] 缩略图删除相关两处后续修复定型：①_resolve_main_thumbnail 回落链改为 thumb_main.txt > thumbnail.jpg > 编号最小的 thumbnail_N.jpg（pages.py has_thumbnail = 白名单 thumb_count>0，不再只查 thumbnail.jpg）——删主图后其余缩略图自动顶上；②缩略图 URL 统一带 ?v={st_mtime_ns} 版本参数（pages.py thumbnail_url + /thumbnails 列表逐项），主图/单图端点 Cache-Control: no-cache、列表 no-store——同 URL 换文件必须破浏览器启发式缓存，否则删图后刷新仍显示旧图。**Why:** 用户连报「删后无图显示」「刷新仍显示删除的缩略图」；弹窗删除按钮在双列窄卡被 overflow-hidden 裁掉（文件名+按钮同行横向溢出），已改上下两行布局。**How to apply:** 弹窗卡片新增操作按钮时保持两行布局勿改回同行 flex；缩略图/任何「同 URL 内容会变」的图片端点都要带 mtime_ns v 参数 + no-cache，不要当冗余 query 清掉；deleteThumb 后统一 loadThumbs + 时间戳戳封面，勿整页 reload。
- [2026-09-02 11:48:44] BGM 版权音乐混合上线（2026-09-02, commit 384d641）：pipeline/bgm_mix.py（移植自 yt_aduio_book_one_to_all_v2/pipeline/bgm.py，入口 mix_bgm_into_video(video_path,...)）；pipeline.py _step55_bgm 在 Step5 与 4K 之间（输出 {标题}_bgm.mp4 新文件，失败/空库回退原片，checkpoint 存 bgm_output，4K 以混音版为源）；配置页「🎵BGM 音乐」组 17 参数（bgm_mix 默认关、全模式生效，music_dir 默认项目根/bgm_music/ 已 gitignore）；运行历史「🎵 混BGM」按钮 POST /api/runs/{name}/bgm_mix → pipeline_service.bgm_mix 后台线程（run_mutex owner="bgm_mix"，参数读运行所在模式 load_mode_config，成片定位排除 _4K/_bgm 防嵌套混音）。关键设计：sidechain 的 intro_outro_seconds 音轨加首尾静音时视频必须 tpad 冻结延展同秒数（否则音画错位 5s，此路径重编码 libx264）；ducking_mode=sidechain 有意关闭 dyn_vol/spec_shape/高通以保留 Content ID 频率指纹，勿"优化"回。**How to apply:** 改 BGM 逻辑只动 bgm_mix.py；三个 compose 函数与 mode_test 均不接入 Step5.5 属预期。
- [2026-09-02 11:49:02] 2026-09-02 并行会话冲突实录：本会话实现 BGM 功能期间，另一会话两次向 pipeline/bgm_mix.py 追加自己的完整重复实现（后定义遮蔽前者；首次测试其版本带 UnboundLocalError），经用户拍板「保留本会话版本」，用临时脚本按唯一锚点（我的 _ffmpeg_overlay_video 末尾 _cleanup(bgm_temp_path) 首次出现处）裁掉追加段（772-1094 行）后正常提交。其余文件（pipeline.py/config_manager.py/pipeline_service.py/runs.py/runs.html）未受污染。**How to apply:** 多会话并行改同一新文件时，写入前先查 mtime+重复定义计数；commit 前必须重新核对（本次 push 前复核 mix_defs==1）。CODELY.md/configs/active_mode.json 等当时仍有另一会话未提交改动，未卷入本提交属预期。
- [2026-09-02 13:04:46] BGM 黑屏根因与 4K 混BGM 按钮定型（2026-09-02, commit 47af455）：①sidechain 首尾 padding 的 tpad 必须同时显式 start_mode=clone + stop_mode=clone——start_mode 默认 add 补黑色帧（正是「BGM 混音前 5 秒黑屏」根因，intro_outro_seconds=5 精确对应 5s 黑屏）；②bgm_mix 新增 ffmpeg_timeout 参数（默认 600 零行为变化）：4K 带 padding 需整片重编码 600s 必超时，服务层 target=4k 时传 3600；③运行历史「🎵 混BGM 4K」按钮（has_4k 才显示）→ POST /api/runs/{name}/bgm_mix_4k → bgm_mix(target="4k")：源={safe}_4K.mp4 兜底最新 *_4K.mp4，输出 {标题}_4K_bgm.mp4——该命名以 _bgm.mp4 结尾天然不匹配 *_4K.mp4 glob，防 has_4k 污染与嵌套混音；④4K 混音走 bgm_mix 互斥锁与单槽线程，前端 _bgmBusy/_bgmTarget 两按钮共用一套轮询。**How to apply:** 后续任何 tpad 首尾延展场景都要检查 start_mode；给 _4K_bgm.mp4 之后的产物命名时保持「源名+_bgm」后缀约定，勿发明 _bgm_4K 后缀（会撞 *_4K.mp4 glob）。
- [2026-09-02 13:41:22] BGM 起始章节 + 配置按混音模式分组上线（2026-09-02, commit 2c92b57）：①chapter_start_seconds 从 youtube_metadata.json 的 chapters（"MM:SS Label"/"HH:MM:SS Label"）解析第 N 章起始秒，任何失败（chapter<=1/缺失/越界/解析错）回退 0.0 从头混；②mix_bgm_into_video(bgm_start_seconds=)：BGM 轨前置静音+衔接 fade_in+片尾补 fade_out，起点超片长防呆回退；pad 重构 pad_start/pad_end——bgm_start>0 时无首部参考段（起点前天然干净、视频头部不延展、章节时间戳对齐），尾部参考段保留，双 0 时视频流回 -c:v copy；**关键坑：lead 偏移只含 bgm_start_seconds 不含 pad_start**（bgm_start>0 时 pad_start 恒 0，bgm_start=0 时 lead 必须为 0——首部参考段需 BGM 盖住，误加 pad_start 实测头部 -91dB 全静音）；③配置 bgm_start_chapter（bgm 组，默认 1=从头）三层接线（PARAM_SPEC/--bgm-start-chapter/_build_args+线程内解析）；④配置页 17 BGM 参数拆三卡：bgm 通用 / bgm_amix（音量偏移/动态音量/频谱塑形/高通）/ bgm_sidechain（基础增益/首尾段/sc 五参），config.html 卡片 id="group-card-{key}" + toggleBGMGroups 按混音模式显隐（隐藏卡字段仍保存，照 toggleTTSFields 先例）；修正 bgm_stereo_offset help（sidechain 同样生效）。**How to apply:** 验证 BGM 起播用频段隔离测量（BGM 80Hz/旁白 440Hz + 双低通 150 滤镜测 mean_volume），别用整轨 mean_volume（BGM 设计上低于旁白 15-25dB，整轨测不出差异）；新 bgm 参数归组时按 amix/sidechain/通用 对应移入三组。
- [2026-09-02 13:56:29] 运行历史「后台任务」悬浮面板上线（2026-09-02, commit 705de83）：runs.html 固定右下角任务面板解决「4K/BGM/缩略图按钮点击后无信息输出、只有出错才弹窗」——双数据源设计：pipeline 主通道（/api/run/logs/since/{idx}，status.get_progress 含 current_step_label/elapsed）与缩略图独立子进程通道（/api/thumbnail_regen/status?since=，含 out_name 无 elapsed），taskWidgetStart(source) 切换源；生成4K/混BGM/混BGM4K/缩略图启动成功后自动展开，页面加载 IIFE 探测已运行任务（含控制台启动的主 pipeline），完成/失败 6s 自动收起；各按钮原轮询/复位/reload 逻辑零改动（面板纯增量）；重渲字幕有意不接入（弹窗自带日志区）。**How to apply:** 后续在 runs 页新增本地渲染按钮时，成功路径加一行 taskWidgetStart('pipeline'|'thumb') 即得实时日志；新任务源若形状与 {logs,total,status} 对齐可直接挂到 _taskWidgetFetch 分支。
- [2026-09-02 14:07:24] BGM 完成后弹出文件而非刷新页面（2026-09-02, commit 0d6da8b）：用户明确要求 BGM 混音完成后不要 location.reload——pollBgmGen 完成分支改为复位按钮 + 从 st.final_path（get_progress 含该字段）取产物名调 openRunFile；open_folder 端点新增 file 参数（explorer /select 高亮选中文件，_raise_folder_window 置前台兼容；仅接受运行目录内普通文件名，Path(file).name != file + 禁分隔符/点段校验置于 run 查找之前）；文件缺失回落打开目录。**Why:** 刷新页面会关掉悬浮任务面板丢失日志，且用户想立刻拿到文件。**How to apply:** 后续其他本地渲染按钮（如 4K）若也要"完成弹文件"照此模式：完成分支读 st.final_path basename → openRunFile；注意不 reload 时卡片视频列表需手动刷新才出现新文件，属用户接受的行为。
- [2026-09-02 14:55:42] 运行历史批量操作定型（2026-09-02, commit 30e6f53）：批量条=删除/标记已上传/生成4K/混BGM 四动作，batchAction(kind) 逐项执行。关键设计约束：①4K/BGM 共用 PipelineService 互斥锁（owner=4k_gen/bgm_mix），批量必须前端串行排队（_batchWaitIdle 轮询 /api/run/logs/since 至非 running），不能并发；②pipeline_service 存在「状态先置 done、互斥锁在线程 finally 才释放」的毫秒级窗口 + is_running=线程存活，下个任务 POST 会瞬时 409，前端按错误文案含「资源被占用/正在运行中」重试 3 次（3s 间隔）；③批量期间每个任务前必须 taskWidgetStart('pipeline') 重置悬浮面板，否则上个任务的 6s 自动收起 hideTimer 会中断显示；④后端 mark_uploaded 新增 state=uploaded/pending 直接设置语义（幂等），缺省保持切换；⑤单项与批量互斥：generate4k/generateBgm 查 _batchBusy，batchAction 查 _fourKBusy/_bgmBusy。BGM 完成双弹窗根因与修法：pollBgmGen 轮询链共享单个 _bgmTimer 变量，任何一条链 catch 重排会覆盖另一条链的 pending id 使 clearTimeout 失效而多链化→完成分支加一次性守卫（!_bgmBusy 直接 return）+ 两条重排路径均以 _bgmBusy 为条件。**Why:** 用户要求多选不止删除还能批量 4K/BGM，且实测 BGM 完成弹了两次资源管理器窗口。**How to apply:** 给 runs 页新增批量动作或改 4K/BGM 任务执行器时保持串行排队+重试+面板重置三件套；凡「完成时弹窗/开窗」的轮询器一律加一次性完成守卫；批量缩略图/重渲字幕有意未做（耗积分/需逐运行选参）。
- [2026-09-02 22:02:35] HTML data 属性含「连字符+数字」不做 dataset 驼峰转换：data-has-4k 的 dataset 键是 "has-4k"（规则只对连字符后的小写字母转大写，数字不触发），JS 读 dataset.has4k 得 undefined 且无报错——曾致批量混BGM 4K 把所有运行误判为无 4K（commit 0b845d0）。**Why:** HTML DOMStringMap 命名规则，Chrome 严格遵循。**How to apply:** 本项目模板里 data-* 属性要被 dataset.xxx 读取时避免连字符后跟数字（用 data-has4k 这种拼写）；同类风险如 data-16x9、data-p3；排查「dataset 读到 undefined」先检查属性名拼写规则。
- [2026-09-03 01:31:34] [2026-09-03] 脚本质量增强四开关上线（commit 134ab56）：script_style_boost / script_outline_first / script_engagement_qa / script_candidates（PARAM_SPEC llm 组，env=SCRIPT_STYLE_BOOST/SCRIPT_OUTLINE_FIRST/SCRIPT_ENGAGEMENT_QA/SCRIPT_CANDIDATES），默认全关=原生成流程逐字节不变。**Why:** 用户报脚本「语言不地道/内容套路化/对话缺张力」，明确要求四个改进方向各自独立实现、逐个开关测试对比效果（原话「这4种都单独生成，原生成流程代码保留不变，让我一个一个测试」），勿擅自把默认值改开或合并开关；A 在 boost 分支才移除生成 prompt 的 'not exaggerated or melodramatic' 压制句；B 对 quest no-op（quest Phase A 本就是大纲先行）；D 候选只跑程序化门禁打分（温度 0.8/0.85/0.9），winner 才进完整 LLM QA。**How to apply:** 后续调脚本生成质量只增强各开关分支内容；few-shot 示例/情节装置/禁开场白共享工具在 pipeline/script_style.py（llm_client 与 quest 共用）；C 张力评审每轮最多 3 段防重写抖动；真实 LLM 效果测试由用户 UI 逐开关验证。
- [2026-09-03 02:01:22] [2026-09-03] pose 图集切分内容感知路径上线（commit f321aea，atlas_split.py）：无缝图集（无实线分隔，is_segmentation 透明图集）不再回退等分盲切——按内容掩码做行/列投影找内容带、在相邻带空隙中心切分，且每个行条带独立求列边界（容忍上下行人物左右错位，等分线劈进旁人是旧版主因）；透明图集格内按 alpha bbox 收紧贴住内容。分层回退：实线图集（全部内部边界检出分隔沟）保持旧逻辑零变化，内容感知失败逐条回退等分。**有意设计勿当 bug 修**：①切分输出尺寸不再统一（bbox 紧贴裁剪）——下游 stop_motion.normalize_pose 对每张姿势独立缩放到统一画布、morph 光流发生在归一化之后，尺寸自适应已验证；②外缘贴内容（人物下摆/肩贴图集边）是 AI 排版本身，非切割错误，验证时只断言内部切割边干净。**Why:** 用户报"分割 pose 会把旁边的 pose 也切割一部分"，实测 test/pose_atlas_char_b.png 内部切割边贴边内容 0.2628→0.0000。**How to apply:** 调切分行为只改 atlas_split.py（split_atlas 签名不变，characters.py/QuestAtlas/SceneAtlas 三个调用方透明受益）；测试图在 test/pose_atlas_char_b.png，验证输出目录 test/split_test_output/（已 gitignore）。
- [2026-09-03 13:11:40] [2026-09-03] 主持人形象与演播室背景已解绑（commit 183c522）：_reuse_characters 素材库分支与 run 复用分支的 host_bg.png 复制块已删除——绑定主持人只带姿势图集，不再夹带背景；host_bg.png 进入运行目录的唯一途径 = _apply_host_bg_binding（host_bg_source=lib:/run:），空值 = 每期按 固定 prompt > script.host_bg_prompt > 默认 自动生成。**Why:** 用户报「每期自动生成（按主题）」不生效，根因是素材库 host 条目（char_1787494036_host）自带 host_bg.png 被无条件夹带进每次新运行 + Step 2 生图对已存在文件跳过；用户拍板解绑方案（否决了「下拉为权威开关」的门控方案）。**How to apply:** 后续勿把复用/素材库分支「缺少 host_bg 复制」当回归补回；素材库保存端点（characters.py）仍随 host 存 host_bg.png——它是背景下拉「素材库」组 has_host_bg 的数据来源，勿删；cutout 保存不存 host_bg 的既有缺口仍未修（用户用 run: 绑定替代）。
- [2026-09-03 13:36:26] SEO 词池上线（commit d66ba1a）：pipeline/seo_keywords.json（core 14 + by_topic 16 组，来源 12 个竞品种子词 + LLM 一次性扩展 + 人工精选）+ pipeline/seo_pool.py（load_pool/merge_tags/keyword_hint）。①thumbnail_gen.save_youtube_metadata 把词池并入 metadata 的 tags 字段：脚本 LLM tags 优先 → by_topic 主题命中 → core，大小写去重、500 字符（YouTube 硬限）按 tag 粒度截断；hashtag 追加有意仍只用脚本原 tags（用户已定「全部追加不去重」规则，勿把词池 tag 也变 hashtag）。②llm_client._seo_hint_line(topic) 在 youtube_tags prompt 行注入 ≤12 个命中搜索短语（listening 直接用、quest 经 import 复用），空池=空串=prompt 逐字节不变。③词池文件缺失/损坏回退空池，merge no-op——零行为变化是设计，勿当 bug 补。④无常驻关键词挖掘脚本（用户明确否决）；扩词池直接编辑 seo_keywords.json。⑤topics.json 现 29 分类 784 条（新增 功能语言 28 / 课堂用语 16 / 演讲汇报 14），CODELY.md 正文「726 条 26 分类」已过时待订正。本机实测：Google suggest 不通；GitHub push 偶发 schannel SSL 握手失败、直接重试即成功。
- [2026-09-03 21:09:36] 控制台批量生成视频队列上线（2026-09-03, commit a0e8880）：app/batch_queue_service.py（单例+worker 线程串行调 PipelineService）+ app/routers/batch_queue.py（add/status/start/pause/resume/remove/clear/stop_current）+ dashboard.html「📦 批量生成队列」卡（脚本多选弹窗/主题多行/随机抽取/2s 轮询）。来源=脚本库脚本（模式随脚本 structure）+ 主题（跟随加入时激活模式）；配置在任务开始时 load_mode_config(mode)+覆盖解析。关键语义（勿当 bug 修）：①后端驻留队列落盘 configs/batch_queue.json（gitignored，重启 running→paused、进行中项标 interrupted）；②暂停=当前视频完成后生效，error 才自动跳过继续，手动停止当前→stopped+队列转暂停；③run_mutex 被占时 5s 重试等待不判失败；④队列 running 时 POST /api/run/start 返回 409 拒绝手动单次启动；⑤SSE done 的确认/错误弹窗在 bqIsActive() 时抑制，队列轮询在 pipeline 运行且 evtSource 为空时自动重连日志流；⑥前端 _bqBlockedManual 只恢复自己禁用的按钮（防与 SSE 启停按钮逻辑打架）；⑦暂停发生在等资源阶段时任务回退 pending。**How to apply:** 改批量队列只动 batch_queue_service.py；新增队列项类型照 add_items/_build_config 模式扩展；真实生成链路由用户 UI 验证（自动测试用 FakeService monkeypatch pipeline_service.get_service）。
- [2026-09-04 12:46:13] [2026-09-04] 后台任务悬浮面板竞态修复定型（commit a9befb5）：批量逐项执行时，上一任务 done 间隙 + 下一项 taskWidgetStart 后首次轮询先于新 POST 到达服务端会读到 stale done → 原 done 分支挂 6s 收起且轮询链终止、running 分支不清 hideTimer → 面板在新任务 ~6s 后消失且批量看似卡住，刷新页面才恢复。修复：①_taskWidget.seq 代数号（taskWidgetStart/taskWidgetHide 各 seq++ 作废 in-flight 响应，then/catch 均校验 source+seq）；②_clearWidgetTimers 统一先清再挂（防孤儿定时器）；③running 分支清除挂起 hideTimer；④done/error 分支仅在不挂时挂 hideTimer（不后延）且继续重排轮询（2s），使 stale-done 下一拍读到 running 取消收起；⑤batchAction 的 taskWidgetStart 从 POST 前移到 POST 成功后。**Why:** 用户报批量运行中面板突然消失。**How to apply:** 后续给面板加数据源/改轮询必须保持 seq+先清后挂纪律；批量/单项启动入口一律「POST 成功后再 taskWidgetStart」（取代 30e6f53 记录中「每个任务前重置面板」的旧约定）。
- [2026-09-04 18:00:37] [2026-09-04] 运行历史批量（生成4K/混BGM/混BGM 4K）改后端驻留队列定型（commit b206ecf）：app/local_batch_service.py 单槽 worker 串行调用 PipelineService.generate_4k/bgm_mix 并轮询 get_progress 至终态（互斥占用重试 10×3s 后标记该项失败继续下一项）；端点用独立顶层域 /api/local_batch/start|status|stop（勿挪进 /api/runs/{name} 通配组，遮蔽陷阱）；stop=当前项完成后停止、剩余标 skipped；队列仅内存不落盘（服务重启即清空，正在渲染的任务随重启终止属既有行为）。前端 batchAction 改提交清单+pollLocalBatch 轮询（current 变化时 taskWidgetStart('pipeline)，遵守 seq 纪律），页面加载 IIFE 探测 running 恢复进度条；批量条新增「⏹ 停止批量」；标记已上传瞬时操作保持前端直发；_batchWaitIdle 已删除。**Why:** 用户实测页面缩到后台批量停摆（浏览器节流后台标签页定时器，隐藏约5分钟后最快1次/分钟，内存节省还可能冻结标签页）。**How to apply:** 后续给批量加新类型照 _call_item 分支扩展；前端轮询显示允许被节流（只影响显示不影响执行）；不要把「批量进行中刷新页面批量继续跑」当 bug。
- [2026-09-04 23:36:30] [2026-09-04] 游戏角色式序列帧动画 sprite_sequence 上线并收敛为 B 单路线（commits 96e3fd1/4ee3099/6bc602e → 三路线全链路实测（test2/ 保留产物：B/C 一次通过、A 修 seedream 1677 万像素上限 400 后通过）→ 用户看对比视频拍板 d735aa5 只留 video_frames 删净 A/C）：①素材生成唯一路线=Seedance2 参考图白底动作视频 → ffmpeg 8fps 抽帧 → 均匀取样 16 帧 → 抠图，4 动作 talking/idle/gesture/wave ×16 帧；generate_sprite_clips 已无 source 参数；参考图一致性链（优先 pose_char_*_0.png，否则用先生成的 talking 帧），manifest=images/sprite_clips.json（{chars:{char:{action:[帧]}}}+fps=12）+ 文件级续传；②sprite_seq._unify_clip_frames 整组几何统一（union bbox+共同 scale+居中 POSE 画布），逐帧独立 normalize 会忽大忽小；③渲染层 _render_sm_segment clips 分支（说话者 talking 基础循环+音频低 RMS 停顿点轮转插入 gesture/wave 一个循环，倾听者 idle 循环替代呼吸正弦；采样 floor(t*clip_fps)%len，clip_fps=12 与 render_fps 解耦；sprite 模式停用光流 morph；无 clips 键=旧姿势路径逐字节不变）；④配置仅 animation 增 sprite_sequence 值（modes=cutout+quest），sprite_seq_source 参数已随 A/C 删除（PARAM_SPEC/build_cli_args/_build_args/argparse 全清）；mode_test 零额外接线（素材走 _step2_images_tts、合成回放 manifest animation 快照）。**Why:** 用户实测三路线后拍板只留 B——帧间一致性/流畅度最好；A/C 相关代码（含 atlas_split.split_atlas_sequence/_cell_alpha_bbox、sensenova SIZE_MAP atlas_seq）已删净。**How to apply:** 后续勿恢复 atlas_grid/sprite_sheet 路线或 split_atlas_sequence；缺失动作/角色自动回退姿势图集是设计行为；教训保留：seedream 生图像素上限 16777216（4992x4992 会 400，方形网格用 4096x4096）。
- [2026-09-05 00:05:15] [2026-09-05] animation 选项收敛定型（commit a97e7a3）：cutout/quest 配置页/CLI 默认只留 stop_motion + sprite_sequence 两项（PARAM_SPEC default=stop_motion，argparse default 同步、choices 保留 none/landing 兼容旧调用）。归一规则：none/landing → stop_motion（cutout/quest 渲染器不读该值，三值渲染等效、素材结构同为姿势图集），实现两处语义一致——pipeline.pipeline._norm_animation（main 解析后归一、resume anim_changed 比较、step0 参数 warning 对比均过它）与 app/config_manager.normalize_animation（build_cli_args/_build_args/mode_test manifest 回放，pipeline 自包含故独立实现）。**Why:** 用户拍板收敛 2 选项；旧 cutout checkpoint（landing/none）resume 不再误判 "Animation changed → starting fresh"。**How to apply:** 后续勿把 legacy 值从 argparse choices 删掉（破坏旧脚本）；video_compose.compose_image 的 landing/stop_motion 分支与 dashboard quick-anim（static 专用死 UI）有意不动；animation 值唯一真实分支仍是 `== "sprite_sequence"` 门控（Step2 素材生成 / Step5 load_clip_map）。
- [2026-09-05 01:12:29] [2026-09-05] 序列帧两个新模式上线：original_cutout_sprite / quest_sprite（commits 74683ed/c3e95c4/1748b61/edbd5ea/b9238d8/6844b41）+ talking 生成 6 秒（原 5s）。**设计**：①行为族归一——config_manager.structure_family(mode) 把新模式映射回 original_cutout/quest，107 处结构分支零改动；模式身份存 args.mode_name（main()/_build_args 双处归一，输出目录/checkpoint/配置分离），args.structure=族名，checkpoint structure 存族名；②每角色 6 clips：talking_01/02/03（48 全帧=6s@8fps，整句单 take 铺放用）+ idle_01/02（16 帧）+ wave（host outro 送别）；gesture 已删（插入机制新模式不用）；③渲染 _render_sm_segment 层键 take_mode：说话者 idx=floor(fidx/total×len) 单 take 铺满整句（无循环/插入/landing），倾听者 idle 变体按 seed 循环，take_action 显式指定（outro=wave）；_build_clip_schedule/_clip_variants 变体组按行轮换兼容旧单键 manifest；④素材库存取：api_library_save 把运行 clips 按动作复制入库（clip_{action}_{j}.png 去角色名）+ 库 sprite_clips.json（desc_snapshot 外观快照）+ meta.sprite_clips；_reuse_characters 两分支（素材库/run 来源）复制改名回运行+manifest 合并→generate 文件级续传跳过=零积分；desc 守卫警告；characters.html「🎬 N 段动作」徽章（library_io clip_count）；⑤mode_test 接入（MINI_LINES 派生 MODES，素材生成不再覆盖 args.structure）。**Why:** 用户拍板复制两新模式承载新功能（原模式不动）+ 整句单 take（比循环+插入更流畅）+ talking 6s 覆盖台词 4-8s 速度带 0.75-1.5x；库存化是新变体成本前提（新角色一次性 6 视频，之后所有 run 零积分）。**How to apply:** 新模式强制 animation=sprite_sequence（main/_build_args 双处），animation PARAM_SPEC modes 未加新模式（配置页隐藏）；后续勿在新模式下走 loop+inserts 旧语义；take 帧取样=时间均匀铺放非定率。
- [2026-09-05 11:27:14] [2026-09-05] 人物素材库「序列帧角色」双路线上线（commit ba4aedf）：①卡片「🎬 生成序列帧」=MCP 自动路线（页面级 PageMcpSession 隔离会话，Seedance2 白底动作视频→本地抽帧抠图入库；普通 5 段 talking×3+idle×2、is_host 或 source_key=host 加 wave 共 6 段；文件级续传；单动作失败继续）；②卡片「📋 提示词」弹窗=外部工具零积分路线（/gen_prompts 输出参考图+各动作完整 prompt 与 MCP 生成逐字一致；import_pose_image 上传图集自动 split_atlas 切 8 姿势+thumb / import_clip 上传视频本地抽帧取样抠图入库，覆盖=重生成）。**Why:** 用户要 sprite 新模式角色素材能直接在素材库建（跑整条管线才产 clips 太贵），且要求支持复制提示词到外部工具（即梦等）再传回。**How to apply:** 改参考图/动作 prompt 必须只改共享源（characters.py _build_image_prompt 与 sprite_seq._video_prompt），gen_prompts 无独立 prompt 文本；_produce_clips_from_video/_refresh_clip_manifest 是两路线共享收尾（manifest 仅登记整组帧齐的动作，fps=12/source=video_frames）；reupload_for_cdn 新增 call_tool_fn 注入参数是页面会话专用，管线调用点勿动；主持人标记=meta.is_host（PUT 更新键已加），编辑表单 checkbox 切换。
- [2026-09-05 12:59:59] [2026-09-05] 素材库序列帧上传体验增量（commit bb870e0）：①重传替换——_produce_clips_from_video 去掉「帧已齐早退」，import_clip 重传始终覆盖旧帧（MCP 自动路线续传跳过仍由 _generate_char_clips 调用方判断，行为不变）；②弹窗每行「📂 打开目录」→ POST /api/character_library/{lib_id}/open_folder?file=（explorer /select 高亮 + 复用 runs.py _list_explorer_windows/_raise_folder_window 置前台；lib_id 须在 LIBRARY_DIR 内、file 仅普通文件名）；③上传成功 alert 区分首次/已替换旧素材，gen_prompts 的 exists/has_ref/sample_file 驱动行内「✅ 已入库」标记刷新；④有参考图（pose_char_a_0.png/char_scene.png）时视频 prompt 附加 _REF_CONSISTENCY 人物一致性强约束——characters.py 的 _with_ref_consistency 封装，gen_prompts 与库内 MCP worker 共用；管线 sprite_seq._video_prompt 保持零改动。**How to apply:** 后续改一致性约束文案只动 _REF_CONSISTENCY；勿把早退删除断当回归补回。
- [2026-09-05 13:21:53] [2026-09-05] 有参考图时视频 prompt 省略外观描述（commit 03734c5）：sprite_seq._video_prompt 的 char_desc 支持空串（省略描述段，避免文字描述与参考图打架干扰一致性）；素材库 gen_prompts 与 MCP worker 在有参考图（pose/scene/talking_01 帧兜底）时传空描述+附加 _REF_CONSISTENCY，无参考图回退完整描述。管线调用方恒传非空描述 → 管线 prompt 输出逐字节不变（冒烟已验证 byte-identical）。**How to apply:** 改 prompt 模板只动 _video_prompt 单一来源；勿在素材库路线恢复描述段。
- [2026-09-05 16:35:41] [2026-09-05] 序列帧素材库最少集+时长自适应定型（commits d942f37/7dce44d）：①库导入按上传视频实际时长全程抽帧（8fps，上限 MAX_CLIP_FRAMES=120，重传替换先清旧帧），帧数不再固定 48/16——管内 MCP 自动路线仍固定 6s→48 帧（sprite_seq 帧数规格未动），manifest/续传/gen_prompts 按实际帧判定，帧号排序必须数值序（j≥100 进位）；②用户拍板轮换语义=「随机但不与上一行重复」，实现=pipeline/stop_motion.pick_take_variant 无状态逐行模拟（raw 随机→与上一行最终结果相同则+1），勿"优化"成单次随机（会破坏不相邻约束）；③运行 manifest 新增 from_library 标记（pipeline_service._merge_run_clip_manifest 库分支写入）：generate_sprite_clips 对这些角色缺失动作不自动 MCP 补齐（用户明确「缺什么用什么，不用自动补齐和回退」，零积分），已有动作沿用原 manifest 帧清单（固定 48/16 续传判定会截断长视频素材）；④倾听者无 idle 变体回退姿势图集路径（旧逻辑 or sorted(clips) 会循环 talking 致静默角色嘴动）。另修 library_io.pose_count 恒 0 bug（`"_c" not in p.stem` 误匹配 pose_char_a 的 _char 前缀——徽章「未生成」根因）。**How to apply:** 后续勿把固定帧数当入库规格加回；用户现有角色 char_1788584757_char_a 仅 talking_01(48帧) 属预期最少集状态。
- [2026-09-06 12:45:55] [2026-09-05] 序列帧三项迭代定型（commits 3661e58/371657b，后续 6b82f8e 订正）：①抽帧密度 8fps→24fps（库导入+管线 MCP 路线统一，sprite_seq.EXTRACT_FPS=24）：全动作（含 idle）全时长全帧（10s→240 帧），库导入上限 MAX_CLIP_FRAMES=240；manifest fps 12→24（循环播放帧率=抽帧密度 → idle 循环原速），旧 fps=12 manifest 兼容不受影响；②wave 仅结尾送别（用户拍板）：_CLIP_INSERTS 移除 wave（讲话段零插入），take_mode 说话者无 talking 变体回退姿势图集（不落 wave），主持人 outro 新旧模式都强制 wave take（take_action 显式传入时无视 sprite_take_mode 门控），主持人动作集去 idle（_CLIP_ACTIONS_HOST=talking×3+wave；sprite_seq.actions_for_char 同步）——注意 _CLIP_ACTIONS_HOST 与 _BASE 互不为超集，任何全动作扫描必须按 is_host 选清单（_refresh_clip_manifest 曾因此漏登记非主持人 idle）；已有库角色的 idle 段成死数据勿当 bug；③「⚡ 快速测试」quick_test 配置（mcp 组全模式）三层接线：Step0 复用同模式最近运行脚本（族回退+标题命名+_qt 后缀防撞）、Step1 跳 MCP、Step2 复制源运行 images/audio（original 加 clips/）+缺失图深灰黑 1280x720 占位+manifest 路径重映射+from_library 全角色（杜绝补齐）、音频缺则 TTS 只补音频（三引擎均本地零积分）、Step3 original 缺失 clip 黑底 lavfi 占位（-t 控时长勿 -shortest）、Step4.5 同 no_thumbnail。④帧登记统一磁盘 glob（≥4 帧即完整，固定帧数判定与补齐逻辑已删——补齐会让短视频变慢动作且污染 manifest）；素材库「📂 打开目录」前端 3 秒防抖（explorer 启动慢连点双开，后端单次调用实证只开一窗）。**How to apply:** 24fps 下旧运行 resume 会把序列帧判未完成重新生成（耗积分，已告知用户）；quick_test 源查找排除 ./_ 前缀目录，helper 兼容 args.output_dir（Web）与 args.output（CLI）。⑤[2026-09-05, commit 027d115] 重大死代码修复：_render_sm_segment 构建 processed_layers 时曾只保留 poses/clips/clip_fps/is_speaker 四键，take_mode/take_action 被丢弃 → 整句单 take 分支自 sprite 模式上线起从未执行（对白/主持人全部走循环或姿势回退，wave 送别/跨行随机轮换失效——用户「wave 没用上」报告的真正根因）。修复后 take 分支首次激活：outro=wave take、对白说话者=talking take 铺放+随机轮换、倾听者 idle 循环。功能验证法：4 色纯色帧 monkeypatch paste_with_shadow 捕获序列——take=单调铺放 0,0,1,1,2,2,3,3，循环=交替 0,2,0,2。
- [2026-09-07 11:06:33] [2026-09-07] 新模式 original_sprite 上线（commit 3d20652，用户拍板命名与进标签行）：original_cutout 固定机位版——对话双角色按身份定死左右位（char_a 恒左 x=0.35 / char_b 恒右 x=0.65，不随说话者换位），is_speaker 仍驱动 talking take/idle 循环。实现要点：①复用 74683ed 族归一先例，structure_family→original_cutout，main()/_build_args 双处强制 animation=sprite_sequence（PARAM_SPEC animation 有意不加新模式）；②渲染层固定机位走 layer["fixed_x"] 键——original_cutout_compose._prepare_segment dialogue 分支 fixed_positions=True 时按 (char_a,char_b) 固定序打 fixed_x 并置 direction=1，video_compose_quest processed_layers 透传 fixed_x（勿丢，同 take_mode 教训），positions 首分支「任一层含 fixed_x→逐层取值」；③_apply_host_bg_binding 结构守卫已改 structure_family（修复 original_cutout_sprite/original_sprite 下 host_bg_source 绑定静默 no-op 的既有缺口）；④新模式进 MODE_LABELS/MODE_SHORT_LABELS（控制台标签行可见，与 sprite 旧模式缺席不同）；dashboard isCutout/host 键/STRUCT_TAGS/完成跳转白名单四处 JS 门已含新模式。**How to apply:** 后续再添"复制某模式的新变体"照此清单走（config_manager 13 处 modes 标注含 options/MODES/labels/family + pipeline.py main + _build_args + mode_test MINI_LINES/端点白名单 + dashboard JS 门）；改固定机位语义只动 fixed_x 两处；quest_sprite 未加标签行属现状勿当 bug。
- [2026-09-07 11:14:15] [2026-09-07] 「⚡ 模式效果测试」页已应用户要求彻底删除（commit ec86a58）：app/mode_test_service.py、app/routers/mode_test.py、app/templates/mode_test.html、main.py 装配、pages.py 页面路由、base.html 侧边栏入口、config_manager normalize_animation docstring 提及，全部清除，代码零残留；output/.mode_test 数据目录本机不存在无需清理。**Why:** 用户原话"把'模式效果测试'删除了，相关代码脚本也要删除干净"。**How to apply:** 旧记忆条目中所有涉及 mode_test 的描述（0c46d1c 上线、normalize_animation "mode_test manifest 回放" 消费方、新增模式清单里 "mode_test MINI_LINES/端点白名单"、seq 面板等）均已失效，勿再引用或恢复该功能；后续新增模式变体的接线清单去掉 mode_test 项即可。
- [2026-09-07 11:51:51] [2026-09-07] 主持人族 hook/outro 文案加长上线（commit cae875f）：llm_client._narration_field_desc(structure, mw) 按结构注入 story_hook/outro 字段说明——original_cutout（sprite 变体生成时已归一为此值）用加长版：hook 60-100 词（场景铺垫+预告 2-3 个听点+hand-off 过渡，明确不重复 welcome 的订阅 CTA）、outro 80-110 词（感想+逐字引用对白 2-3 个核心句复盘+跟读建议+评论/点赞/订阅 CTA）；quality_gate_listening 对 cutout 族加旁白总词数 warning（复用 _words，error 会触发 QA 空转保护故保持 warning）。用户决策：只改主持人三模式，original/static 的静态文字卡与 welcome 一律不动（二者 prompt 已验证与旧版逐字节一致）。渲染层零改动（enrich_timeline 音频自适应时长 + burn_subtitles 逐句拆分本就支持长旁白）。**Why:** 用户报主持人开场结尾"太单薄"，要求实现承接下文/总结上文功能，篇幅拍板对齐 quest。**How to apply:** 调篇幅只改 _narration_field_desc 数值与文案和门禁 (60,100)/(80,110) 两处；旁白不进 QA 修复循环（无行号），实际达标率靠用户真实生成验证；勿把 original/static 的短文案当 bug。
- [2026-09-07 16:08:13] [2026-09-07, commit ab5b9dd] original_sprite 三项体验修复上线。①对话段交叉溶解：compose_original_cutout 在占位段填充后、None 过滤前（此时 segments 下标与 timeline 对齐）扫描连续 dialogue run（≥2 段且文件齐全），_merge_dialogue_xfade 合并为叠化块——除末段外 tpad=stop_mode=clone 尾帧冻结延展 DIALOGUE_XFADE_SEC=0.32s，xfade 链 offset=Σ前面各段 ffprobe 实测视频流时长（_probe_video_duration 新增），块总长=Σ段时长→段 k 画面与音频同点起步、字幕零漂移，音频走 concat 滤镜一次编码，块作为普通均匀段进 concat_segments 三段式流程；min(段时长)<2×xfade 或任一失败 fail-open 回退硬切；生效范围 2026-09-07 二轮扩展（commit 7348324）：叠化改为全模式配置项 dialogue_xfade（checkbox/video 组/默认关，original、original_static、original_cutout、original_cutout_sprite、quest、quest_sprite 六模式可见，original_sprite 不含——sprite 恒开不经开关），实现收进 media_utils.merge_dialogue_runs_xfade 单一来源（_merge_dialogue_xfade 加 fps 参数：cutout/quest=25、listening/image=24；run 判定容忍 original 分组跳行 gap=两段间 timeline 条目全 dialogue；None 段断开 run 防单个失败段废掉整条 run），四合成器统一接线、门控=compose_original_cutout 用 fixed_positions or dialogue_xfade、其余三个用 dialogue_xfade；章节边界（welcome/hook→首句）不叠化。②hook 问候去重：llm_client._dedupe_hook_greeting 在 _generate_listening_raw 末尾仅 structure==original_cutout 时调用，hook 首句与 welcome_en 都命中 _GREETING_RE 才删 hook 首句（单句 hook 不删防空），prompt 的 hook 说明同步禁止再打招呼（仅 cutout 分支，original/static 逐字节不变）。③分句引号兼容：burn_subtitles 嵌套 _split_subtitles 提升为模块级 media_utils._split_narration_subtitles（嵌套名留薄包装调用点零改动），EN 句末标点可带闭引号再断句、ZH 闭引号并入前句单元——outro 按 prompt 要求逐字引用金句，旧正则在"句号+闭引号"处失配导致多句挤成一条巨型字幕（用户报的 outro 拥挤根因）。**Why:** 用户报 original_sprite 三个体验问题（Welcome back 连播两遍/对话切换生硬/outro 字幕拥挤）；过渡方式（0.32s 短叠化）与生效范围（仅 original_sprite）均为用户选项确认。**How to apply:** 调叠化只动 DIALOGUE_XFADE_SEC 与 _merge_dialogue_xfade；其他模式无叠化=dialogue_xfade 开关未开（默认关属设计），勿当 bug；改任一合成器的段规格/fps 时同步核对该合成器 merge 调用的 fps 参数；改 hook 文案规则时同步检查 _dedupe_hook_greeting 触发条件；改分句正则时保持引号兼容且无引号文本分句结果与旧逻辑一致（存量 outro 拥挤 run 可用「重渲字幕」零成本修复）。



### Reference

